# 班薪日历 App 开发设计文档

## 0. 文档用途

| 项目 | 内容 |
|---|---|
| 文档版本 | V1.0 |
| 更新日期 | 2026-08-05 |
| 产品暂定名 | 班薪日历 |
| 目标平台 | Android、iOS |
| 目标读者 | 客户端、服务端、测试、安全及后续接手开发的 AI/工程师 |
| 关联文档 | `功能说明文档.md` |

本文件是可独立用于启动开发的技术基准。后续任务不需要依赖历史对话。

本设计固定以下关键决策：

1. 使用 Flutter 构建跨平台 UI 与业务主体。
2. Android 闹钟使用 Kotlin 原生桥接，iOS 通知使用 Swift 原生桥接。
3. 采用本地优先架构，核心功能无网络可用。
4. SQLite 保存业务数据，API 密钥使用系统安全存储。
5. 排班、工时和工资由确定性本地引擎计算。
6. AI 只能通过受控工具访问业务能力，写操作必须预览和确认。
7. V1.0 不强制账号和自有业务后端；节假日数据服务为唯一必需远端业务接口。

原型文件 `app-prototype.html` 只用于视觉和交互参考，不作为生产代码基础。

---

## 1. 技术目标与约束

### 1.1 技术目标

- Android 与 iOS 共享排班、出勤、统计、工资和 AI 业务代码。
- 闹钟、通知、重启恢复和安全存储使用平台可靠能力。
- 业务计算可单元测试、可重建、可审计。
- 无网时支持排班、日历、打卡、统计、工资和已注册闹钟。
- AI 接口可由用户配置，不锁定单一模型服务商。
- 敏感数据最小出站，API 密钥不进入普通数据库和日志。

### 1.2 支持基线

建议首发基线：

- Android 8.0（API 26）及以上。
- iOS 15 及以上。
- 手机竖屏为首发主形态，平板使用响应式居中布局。
- 中文简体首发，代码和数据结构预留国际化。

具体 Flutter、Dart、Gradle、Kotlin、Swift 和依赖包版本在项目初始化时锁定当前兼容稳定版，并提交锁文件。不得使用 `latest` 或不固定版本的 CI 配置。

---

## 2. 总体架构

### 2.1 架构图

```text
┌──────────────────────── Flutter Presentation ────────────────────────┐
│ 首页 │ 日历 │ AI 助理 │ 统计 │ 设置 │ 弹层/确认/权限引导             │
└──────────────────────────────┬────────────────────────────────────────┘
                               │ ViewModel / Notifier
┌──────────────────────── Application Layer ───────────────────────────┐
│ Use Cases                                                           │
│ ResolveCalendar │ SaveAttendance │ CalculatePayroll │ SyncAlarms     │
│ AskAssistant │ ProposeAIAction │ ConfirmAIAction │ UndoAIAction     │
└──────────────────────────────┬────────────────────────────────────────┘
                               │ Domain interfaces
┌─────────────────────────── Domain Layer ─────────────────────────────┐
│ Schedule Engine │ Attendance Engine │ Wage Engine │ Alarm Policy     │
│ AI Tool Policy │ Permission Policy │ Entities / Value Objects        │
└───────────────┬────────────────┬──────────────────┬───────────────────┘
                │                │                  │
┌──────────── Data ───────────┐  │    ┌──────── Platform Adapters ─────┐
│ SQLite / Repositories       │  │    │ Android: AlarmManager/Kotlin   │
│ Holiday HTTP Client         │  │    │ iOS: UserNotifications/Swift   │
│ LLM Provider Adapters       │  │    │ Keychain / Keystore            │
│ Backup / CSV                │  │    │ Timezone / Lifecycle           │
└─────────────────────────────┘  │    └─────────────────────────────────┘
                               │
                         Deterministic results
```

### 2.2 分层规则

- Presentation 不直接访问数据库、HTTP 或平台通道。
- Application 编排用例和事务，不包含 UI 代码。
- Domain 不依赖 Flutter、SQLite、HTTP 或平台 SDK。
- Data 实现 Domain Repository 接口。
- Platform Adapter 只暴露受控能力，不向业务层泄漏原生对象。
- AI Provider 只返回文本或结构化工具请求，不可直接访问 Repository。

### 2.3 推荐技术组件

| 能力 | 推荐方案 | 说明 |
|---|---|---|
| UI | Flutter Material 3 + 自定义设计令牌 | 同时适配 Android/iOS 安全区 |
| 状态管理 | Riverpod 类单向数据流方案 | Provider 不得承担业务计算 |
| 路由 | 声明式路由 | 支持深链和页面恢复 |
| 数据库 | SQLite + 类型安全 ORM/DAO | 必须支持迁移和事务 |
| HTTP | 支持拦截器、取消、流式响应的客户端 | 节假日和大模型连接共用基础层 |
| JSON | 代码生成的不可变模型 | 禁止大面积手写动态 Map |
| 安全存储 | iOS Keychain / Android Keystore 封装 | 保存 API Key 和敏感请求头 |
| 原生桥接 | Flutter Platform Channel 或 Pigeon 类强类型桥接 | 闹钟、通知和系统能力 |
| 图表 | 本地渲染图表库或 CustomPainter | 图表必须有数值明细 |
| 日志 | 结构化日志 + 脱敏器 | Release 默认不记录敏感内容 |

依赖选型时优先选择维护活跃、许可证明确、支持当前平台基线且有原生能力审计记录的库。

---

## 3. 工程目录设计

建议采用功能优先、分层明确的目录：

```text
lib/
├─ app/
│  ├─ app.dart
│  ├─ router.dart
│  ├─ theme/
│  └─ localization/
├─ core/
│  ├─ database/
│  ├─ network/
│  ├─ secure_storage/
│  ├─ logging/
│  ├─ errors/
│  ├─ time/
│  └─ widgets/
├─ features/
│  ├─ onboarding/
│  ├─ schedule/
│  │  ├─ domain/
│  │  ├─ application/
│  │  ├─ data/
│  │  └─ presentation/
│  ├─ calendar/
│  ├─ attendance/
│  ├─ alarm/
│  ├─ wage/
│  ├─ statistics/
│  ├─ assistant/
│  ├─ holiday/
│  ├─ backup/
│  └─ settings/
└─ platform/
   ├─ alarm_bridge.dart
   ├─ notification_bridge.dart
   └─ lifecycle_bridge.dart

android/app/src/main/kotlin/.../
├─ alarm/
├─ notification/
└─ secure/

ios/Runner/
├─ AlarmBridge.swift
├─ NotificationBridge.swift
└─ SecureStoreBridge.swift

test/
├─ unit/
├─ widget/
├─ integration/
└─ fixtures/
```

### 3.1 模块依赖

```text
calendar → schedule
attendance → schedule
wage → attendance + schedule
statistics → schedule + attendance + wage
alarm → schedule
assistant → application tool gateway
```

禁止 `schedule` 反向依赖 `calendar`、`attendance`、`wage` 或 `assistant`。

---

## 4. 核心领域模型

### 4.1 值对象

建议定义不可变值对象，构造时完成校验：

```text
LocalDate         YYYY-MM-DD，不含时区
LocalTimeOfDay    00:00～23:59
ZonedInstant      UTC 时间戳 + 来源时区
WorkMinutes       非负整数分钟
Money             整数最小货币单位或 Decimal
ShiftId           强类型 ID
RuleId            强类型 ID
DateRange         闭区间，start <= end
```

金额禁止使用二进制浮点直接累计。推荐使用整数“分”或十进制定点类型。

### 4.2 核心实体

```dart
enum DayStatus {
  work,
  adjustedWorkday,
  rest,
  publicHoliday,
  leave,
}

enum DaySource {
  defaultRule,
  scheduleRule,
  officialHoliday,
  companyOverride,
  userOverride,
}

class ResolvedCalendarDay {
  final LocalDate date;
  final DayStatus status;
  final ShiftSnapshot? shift;
  final DaySource source;
  final int plannedPaidMinutes;
  final List<DayTag> tags;
  final int resolverVersion;
}
```

所有跨模块使用的班次必须使用 `ShiftSnapshot`，避免模板被修改后历史含义发生变化。

---

## 5. 本地数据库设计

### 5.1 数据库原则

- SQLite 开启外键、WAL 和事务。
- 所有表包含稳定主键、创建时间和更新时间。
- 业务删除优先软删除；用户明确清空数据时再物理删除。
- 每次 Schema 变更必须提供向前迁移和迁移测试。
- API Key、敏感请求头不进入 SQLite。

### 5.2 主要表

#### `user_settings`

```text
id TEXT PRIMARY KEY
locale TEXT NOT NULL
timezone TEXT NOT NULL
currency TEXT NOT NULL
week_start INTEGER NOT NULL
hour_display_mode TEXT NOT NULL
theme_mode TEXT NOT NULL
holiday_region TEXT NOT NULL
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
```

#### `shift_templates`

```text
id TEXT PRIMARY KEY
name TEXT NOT NULL
short_name TEXT NOT NULL
start_minute INTEGER
end_minute INTEGER
cross_day INTEGER NOT NULL
unpaid_break_minutes INTEGER NOT NULL
planned_paid_minutes INTEGER
color_argb INTEGER NOT NULL
is_workday INTEGER NOT NULL
enabled INTEGER NOT NULL
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
deleted_at INTEGER
```

#### `schedule_rules`

```text
id TEXT PRIMARY KEY
name TEXT NOT NULL
rule_type TEXT NOT NULL
anchor_date TEXT NOT NULL
cycle_length_days INTEGER
cycle_payload_json TEXT NOT NULL
effective_start TEXT NOT NULL
effective_end TEXT
priority INTEGER NOT NULL
enabled INTEGER NOT NULL
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
deleted_at INTEGER
```

#### `day_overrides`

```text
id TEXT PRIMARY KEY
work_date TEXT NOT NULL
status TEXT NOT NULL
shift_template_id TEXT
shift_snapshot_json TEXT
override_type TEXT NOT NULL
reason TEXT
note TEXT
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
deleted_at INTEGER
UNIQUE(work_date, deleted_at)
```

#### `holiday_records`

```text
work_date TEXT NOT NULL
region TEXT NOT NULL
name TEXT NOT NULL
day_type TEXT NOT NULL
data_version TEXT NOT NULL
published_at INTEGER
updated_at INTEGER NOT NULL
PRIMARY KEY(work_date, region)
```

#### `calendar_day_cache`

```text
work_date TEXT PRIMARY KEY
resolved_status TEXT NOT NULL
shift_snapshot_json TEXT
source_type TEXT NOT NULL
source_id TEXT
planned_minutes INTEGER NOT NULL
resolver_version INTEGER NOT NULL
input_version TEXT NOT NULL
resolved_at INTEGER NOT NULL
```

该表是可删除重建的缓存，不是业务事实来源。

#### `attendance_records`

```text
id TEXT PRIMARY KEY
work_date TEXT NOT NULL
clock_in_at INTEGER
clock_out_at INTEGER
unpaid_break_minutes INTEGER NOT NULL
source TEXT NOT NULL
status TEXT NOT NULL
edit_reason TEXT
note TEXT
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
deleted_at INTEGER
```

#### `leave_records`

```text
id TEXT PRIMARY KEY
work_date TEXT NOT NULL
leave_type TEXT NOT NULL
start_at INTEGER
end_at INTEGER
paid INTEGER NOT NULL
deduction_minor INTEGER
note TEXT
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
deleted_at INTEGER
```

#### `alarm_templates`

```text
id TEXT PRIMARY KEY
name TEXT NOT NULL
mode TEXT NOT NULL
fixed_minute INTEGER
offset_minutes INTEGER
sound_id TEXT
vibrate INTEGER NOT NULL
snooze_minutes INTEGER NOT NULL
max_snooze_count INTEGER NOT NULL
enabled INTEGER NOT NULL
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
```

#### `alarm_instances`

```text
id TEXT PRIMARY KEY
template_id TEXT NOT NULL
schedule_date TEXT NOT NULL
trigger_at INTEGER NOT NULL
shift_id TEXT
locked INTEGER NOT NULL
platform_alarm_id TEXT
status TEXT NOT NULL
payload_hash TEXT NOT NULL
last_synced_at INTEGER
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
UNIQUE(platform_alarm_id)
```

#### `wage_rules`

```text
id TEXT PRIMARY KEY
mode TEXT NOT NULL
currency TEXT NOT NULL
base_rate_minor INTEGER NOT NULL
overtime_rates_json TEXT NOT NULL
allowance_rules_json TEXT NOT NULL
deduction_rules_json TEXT NOT NULL
period_start_day INTEGER NOT NULL
rounding_rule_json TEXT NOT NULL
effective_start TEXT NOT NULL
effective_end TEXT
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
```

#### `payroll_periods`

```text
id TEXT PRIMARY KEY
start_date TEXT NOT NULL
end_date TEXT NOT NULL
status TEXT NOT NULL
calculated_minor INTEGER NOT NULL
confirmed_minor INTEGER
calculation_snapshot_json TEXT NOT NULL
confirmed_at INTEGER
created_at INTEGER NOT NULL
updated_at INTEGER NOT NULL
UNIQUE(start_date, end_date)
```

#### AI 相关表

```text
ai_provider_configs
  id, provider_type, base_url, model_name, credential_ref,
  custom_headers_ref, timeout_seconds, max_output_tokens,
  stream_enabled, connection_status, last_tested_at, created_at, updated_at

assistant_personas
  id, display_name, preset_type, custom_instruction,
  reply_length, initiative_level, emoji_level, avatar_asset_id, updated_at

conversations
  id, title, model_snapshot_json, created_at, updated_at, archived_at

messages
  id, conversation_id, role, content, content_type,
  tool_call_id, local_only, created_at

ai_actions
  id, conversation_id, action_type, tool_name, proposed_payload_json,
  validated_payload_json, before_snapshot_json, after_snapshot_json,
  status, confirmation_token_hash, input_version,
  confirmed_at, executed_at, undone_at, error_code, created_at
```

`credential_ref` 和 `custom_headers_ref` 只保存安全存储引用。

### 5.3 索引

至少建立：

- `attendance_records(work_date, deleted_at)`。
- `day_overrides(work_date, deleted_at)`。
- `alarm_instances(trigger_at, status)`。
- `holiday_records(region, work_date)`。
- `messages(conversation_id, created_at)`。
- `ai_actions(status, created_at)`。
- `payroll_periods(start_date, end_date)`。

---

## 6. 排班解析引擎

### 6.1 输入

- 默认工作/休息设置。
- 生效中的排班规则。
- 官方节假日数据。
- 公司自定义安排。
- 用户单日覆盖。
- 班次模板快照。
- 请求日期范围和时区。

### 6.2 优先级

```text
user_override
> company_override
> official_holiday
> schedule_rule
> default_rule
```

### 6.3 解析伪代码

```dart
ResolvedCalendarDay resolveDay(LocalDate date, ResolverInput input) {
  final user = input.userOverrideFor(date);
  if (user != null) return fromUserOverride(user);

  final company = input.companyOverrideFor(date);
  if (company != null) return fromCompanyOverride(company);

  final holiday = input.officialHolidayFor(date);
  if (holiday != null) return fromHoliday(holiday, input);

  final rule = input.highestPriorityRuleFor(date);
  if (rule != null) return evaluateRule(rule, date, input);

  return evaluateDefault(date, input);
}
```

### 6.4 周期计算

自定义周期：

```text
offset = daysBetween(anchorDate, targetDate)
normalized = ((offset % cycleLength) + cycleLength) % cycleLength
cycleItem = cycle[normalized]
```

大小周使用 14 天周期或两个明确周模板。不要用自然周序号奇偶直接判断，以免跨年产生偏差。

### 6.5 缓存与失效

- 默认滚动解析过去 12 个月至未来 24 个月。
- UI 查询缺失日期时允许按需解析。
- 规则、模板、节假日版本、时区或覆盖变化后，计算受影响日期范围。
- 删除对应 `calendar_day_cache` 后在同一事务完成业务写入，再异步重建。
- 缓存行保存 `input_version`，读取时发现不一致立即重算。

### 6.6 排班写入事务

```text
BEGIN
  保存规则/覆盖
  使受影响缓存失效
  写入 change_log
COMMIT

重建日历缓存
触发 AlarmSyncUseCase
触发 StatisticsInvalidation
```

闹钟同步失败不得回滚排班事务，但必须进入可重试队列并显示异常。

---

## 7. 出勤与工时引擎

### 7.1 校验

- `clock_out_at` 必须晚于 `clock_in_at`。
- 单段时长不得超过产品允许的上限，建议默认 24 小时，超出要求确认。
- 同一工作日的多段记录不得重叠。
- 休息分钟不得大于该段总分钟。
- 缺少任一端时保存为 `incomplete`，不计算实际工时。

### 7.2 计算

```dart
int paidMinutes(AttendanceSegment segment) {
  if (!segment.isComplete) return 0;
  final total = minutesBetween(segment.clockIn, segment.clockOut);
  return max(0, total - segment.unpaidBreakMinutes);
}

DailyHours calculateDailyHours(
  List<AttendanceSegment> segments,
  int normalLimitMinutes,
) {
  final actual = sum(segments.map(paidMinutes));
  final normal = min(actual, normalLimitMinutes);
  final overtime = max(0, actual - normalLimitMinutes);
  return DailyHours(actual, normal, overtime);
}
```

取整规则只能应用于“计薪工时”，原始实际分钟始终保留。

### 7.3 跨夜归属

- `work_date` 保存班次开始日。
- 原始打卡时间保存 UTC 时间戳。
- 自然日拆分仅在查询层生成，不改原始记录。
- 时区变化时保留原始 `work_date` 和创建时区，避免历史归属漂移。

---

## 8. 工资计算引擎

### 8.1 设计原则

- 输入、输出不可变。
- 所有金额使用整数分或十进制定点。
- 每个结果包含可解释明细。
- 已结算周期保存输入规则、排班、出勤和计算结果快照。
- AI、UI、CSV 共用同一结果对象。

### 8.2 输出模型

```dart
class PayrollResult {
  final Money basePay;
  final Money normalHoursPay;
  final Map<OvertimeType, Money> overtimePay;
  final List<MoneyLine> allowances;
  final List<MoneyLine> deductions;
  final Money estimatedTotal;
  final int normalMinutes;
  final Map<OvertimeType, int> overtimeMinutes;
  final List<CalculationTrace> traces;
}
```

### 8.3 计算顺序

```text
1. 固定计薪周期
2. 读取周期内最终排班和有效出勤
3. 计算原始实际分钟
4. 应用正常/加班分类
5. 应用计薪取整
6. 计算基本工资和加班工资
7. 计算补贴
8. 计算请假、迟到、旷工等扣款
9. 汇总后统一按货币规则舍入
10. 生成 trace 和 snapshot
```

### 8.4 加班类型

```text
workday_overtime
rest_day_overtime
public_holiday_overtime
```

倍率和是否适用完全由用户配置，代码不得硬编码为法律意见。

### 8.5 结算快照

`calculation_snapshot_json` 至少包含：

- 工资规则版本。
- 日期范围和时区。
- 每日排班、实际/计薪分钟和加班类型。
- 补贴及扣款明细。
- 取整规则。
- 总额和计算引擎版本。

---

## 9. 闹钟与通知设计

### 9.1 领域接口

```dart
abstract interface class PlatformAlarmService {
  Future<AlarmCapability> capability();
  Future<void> schedule(PlatformAlarmRequest request);
  Future<void> cancel(String platformAlarmId);
  Future<Set<String>> listManagedAlarmIds();
}
```

平台层只管理本 App 生成的 ID。

### 9.2 闹钟实例生成

```text
ResolvedCalendarDay + ShiftSnapshot + AlarmTemplate
→ 计算 triggerAt
→ 生成稳定 platformAlarmId
→ 与本地 alarm_instances 做差异比较
→ cancel removed / schedule new / keep unchanged
```

稳定 ID 建议：

```text
hash(appNamespace + scheduleDate + shiftId + alarmTemplateId + triggerAt)
```

### 9.3 同步算法

```dart
Future<AlarmSyncResult> sync(DateRange range) async {
  final desired = await buildDesiredAlarmSet(range);
  final existing = await repository.activeInstances(range);

  final toCancel = existing.keys - desired.keys;
  final toCreate = desired.keys - existing.keys;
  final toReplace = changedPayloadHashes(existing, desired);

  cancel(toCancel + toReplace.oldIds);
  schedule(toCreate + toReplace.newRequests);
  persistSyncResult();
}
```

同步必须幂等。重复执行不得创建重复闹钟。

### 9.4 Android 实现

- Kotlin 使用 `AlarmManager`。
- 精确提醒优先使用适合闹钟场景的系统 API。
- 调用前检查精确闹钟能力；权限不足返回结构化状态。
- 注册设备启动、时区变化、系统时间变化和应用更新后的恢复逻辑。
- 响铃页面使用专用通知渠道和平台允许的高优先级展示方式。
- 闹钟触发后将状态写回 Flutter 可读取的持久层或桥接队列。
- 厂商后台限制只能引导用户设置，不承诺完全绕过系统策略。

### 9.5 iOS 实现

- Swift 使用 `UNUserNotificationCenter` 本地通知。
- 维护滚动时间窗口，建议只注册未来 30 天，并在 App 启动/恢复时补充。
- 申请权限前由 Flutter 页面解释用途。
- 静音、专注模式等限制以能力状态和文案说明，不模拟系统时钟权限。
- 待处理通知接近平台限制时优先保留最近和已锁定提醒。

### 9.6 恢复与自检

- App 启动时自检。
- 每日首次进入前台时自检。
- 排班、班次、请假或时区变化后立即同步。
- Android 重启后重建。
- 同步失败保存错误码、重试次数和下一次重试时间。

---

## 10. AI 助理架构

### 10.1 总体流程

```text
Chat UI
  ↓
ConversationService
  ├─ CapabilityContextBuilder
  ├─ DataScopeFilter
  ├─ LLMProviderAdapter
  └─ ToolGateway
        ├─ Query tools → Domain/Application services
        └─ Write tools → Proposal → Validation → Confirmation → Execute
```

### 10.2 Provider 抽象

```dart
abstract interface class LlmProvider {
  Stream<LlmEvent> chat({
    required List<LlmMessage> messages,
    required List<ToolDefinition> tools,
    required LlmRequestOptions options,
  });

  Future<ConnectionTestResult> testConnection();
}
```

首发适配器：

1. `OpenAICompatibleProvider`：兼容常见 chat completions/tool calling 风格。
2. `CustomRestProvider`：通过可配置字段映射接入自定义 REST 接口。

不要在 UI 中写死请求路径。Provider 配置决定路径、鉴权头、请求体和响应解析。

### 10.3 配置与安全存储

普通配置保存在 SQLite：

- provider type
- base URL
- model name
- timeout
- stream enabled
- max output tokens

敏感配置保存在 Keychain/Keystore：

- API Key
- Bearer Token
- 自定义敏感请求头

保存后数据库只记录随机 `credential_ref`。应用日志、异常、备份和分析事件禁止输出密钥。

### 10.4 Base URL 校验

- Release 只允许 `https`。
- 拒绝包含用户名/密码的 URL。
- 规范化尾部 `/`，但不得擅自修改路径主体。
- 域名变化后提示“密钥将发送到新域名”，要求再次确认。
- 测试连接使用最小请求，不写入正式对话。
- TLS、DNS、超时、鉴权、模型不存在、限流和响应格式错误分别映射。

### 10.5 功能知识

在 App 包内维护版本化资源：

```text
assets/assistant/
├─ app_capabilities.zh-CN.json
├─ feature_help.zh-CN.json
├─ tool_definitions.json
└─ safety_rules.zh-CN.md
```

`app_capabilities` 只列当前构建实际支持的能力。系统提示由这些资源动态生成，不在代码中维护大段不可测试字符串。

### 10.6 数据上下文

`DataScopeFilter` 根据用户权限生成最小上下文：

```dart
class AssistantDataScopes {
  final bool scheduleRead;
  final bool attendanceRead;
  final bool wageRead;
  final bool alarmRead;
  final bool notesRead;
}
```

优先发送：

- 确定性汇总。
- 当前问题涉及的日期范围。
- 去除数据库 ID 和无关字段的 DTO。

默认不发送：

- 全量历史记录。
- 工资金额。
- 精确打卡时间。
- 用户备注。
- API 密钥和安全存储内容。

### 10.7 受控工具

首发工具：

```text
get_app_capabilities
get_schedule
propose_schedule_change
apply_schedule_change
get_attendance_summary
get_wage_summary
get_alarm_summary
propose_alarm_change
apply_alarm_change
undo_ai_action
```

查询工具可以在权限允许时直接运行。写工具分为 `propose_*` 和 `apply_*` 两类。

### 10.8 工具参数示例

```json
{
  "name": "propose_schedule_change",
  "arguments": {
    "range": {
      "start": "2026-08-15",
      "end": "2026-08-15"
    },
    "new_status": "rest",
    "shift_id": null,
    "reason": "user_requested",
    "sync_alarms": true
  }
}
```

参数必须通过 JSON Schema、枚举、范围、权限和业务规则校验。自然语言日期必须在进入工具前解析为明确日期，并在预览中显示星期。

### 10.9 确认协议

Proposal 保存：

- 用户 ID/本地 profile ID。
- 会话 ID。
- 结构化 payload。
- 当前数据版本 `input_version`。
- before snapshot。
- 影响日期和记录数量。
- 预期联动结果。
- 过期时间。

确认时生成一次性 token：

```text
token = random256()
stored_hash = HMAC(token, deviceSecret)
```

执行前再次检查：

1. token 有效且未使用。
2. 当前数据版本等于 proposal 的 `input_version`。
3. 用户权限仍有效。
4. payload 再次通过业务校验。
5. 幂等键未执行。

任一失败则使原确认失效并重新生成预览。

### 10.10 写操作事务

```text
BEGIN
  记录 AIAction executing
  保存业务变更
  保存 before/after snapshot
  使缓存失效
  标记 confirmation token 已使用
  标记 AIAction succeeded
COMMIT

同步闹钟和统计缓存
返回真实执行结果
```

数据库事务失败时状态为 `failed`，AI 不得返回成功文案。

### 10.11 撤销

- 仅通过 `before_snapshot` 生成逆操作。
- 撤销也要校验当前数据是否已被再次修改。
- 发生冲突时展示差异，禁止强行覆盖。
- 已撤销 action 不允许重复撤销。

### 10.12 对话安全

- 模型文本按不可信内容渲染，Markdown 使用白名单。
- 禁止执行模型输出的脚本、SQL、命令或任意 URL。
- 工具网关不提供任意数据库查询或任意 HTTP 请求。
- 防止 prompt injection 要求跳过确认、读取密钥或扩大权限。
- 链接必须由用户点击，App 不自动请求模型生成的地址。
- 流式请求支持取消；取消后不执行未完成工具调用。

---

## 11. 网络接口

### 11.1 通用策略

- 设置连接、读取和总超时。
- 支持请求取消。
- GET 可对网络错误做有限指数退避；写请求只有幂等时才自动重试。
- 日志记录请求 ID、耗时和错误类型，不记录敏感正文。
- 客户端不得关闭 TLS 校验。

### 11.2 节假日接口

```http
GET /api/v1/holidays?region=CN&year=2027
If-None-Match: "2027.1"
```

```json
{
  "region": "CN",
  "year": 2027,
  "version": "2027.1",
  "published_at": "2026-12-01T00:00:00+08:00",
  "days": [
    {
      "date": "2027-01-01",
      "name": "元旦",
      "type": "holiday"
    },
    {
      "date": "2027-01-04",
      "name": "元旦调休",
      "type": "adjusted_workday"
    }
  ]
}
```

约束：

- `type` 只允许 `holiday`、`adjusted_workday`。
- 同一地区和日期唯一。
- 使用 ETag 或版本号。
- 响应必须通过 Schema 校验后在事务中替换。
- 数据异常时保留旧版本，不清空本地节假日。

### 11.3 AI 接口

AI 请求由 Provider Adapter 决定，基础请求对象统一为：

```dart
class LlmRequest {
  final List<LlmMessage> messages;
  final List<ToolDefinition> tools;
  final String model;
  final int maxOutputTokens;
  final bool stream;
  final String requestId;
}
```

Provider 响应统一映射为：

```text
text_delta
tool_call_delta
tool_call_completed
usage
completed
provider_error
```

UI 和业务层不得直接解析服务商原始响应。

---

## 12. 状态管理与页面导航

### 12.1 状态分类

```text
持久业务状态：Repository/SQLite
页面可恢复状态：Notifier + route state
短暂 UI 状态：Widget local state
平台能力状态：Capability service stream
```

不要把数据库对象长期复制到全局状态。页面按 ID 订阅 Repository 查询结果。

### 12.2 主要路由

```text
/
/onboarding
/home
/calendar?month=2026-08
/day/2026-08-15
/schedule/rules
/shift/:id
/statistics?range=current_payroll
/assistant
/assistant/conversation/:id
/settings
/settings/alarm
/settings/wage
/settings/holiday
/settings/assistant
/settings/backup
```

### 12.3 页面加载状态

每个异步页面统一支持：

- initial
- loading
- data
- empty
- recoverableError
- blockingError
- permissionRequired

保存操作支持：idle、validating、saving、succeeded、failed。

---

## 13. 备份、恢复与导出

### 13.1 本地备份

- 数据库一致性快照，不直接复制正在写入的 WAL 文件。
- 备份清单包含 schema version、App version、创建时间、数据范围和校验和。
- 至少保留最近 7 份，可配置关闭。
- API 密钥和敏感请求头不进入普通备份。

### 13.2 恢复

```text
校验文件 → 校验 checksum → 检查 schema → 创建当前备份
→ 在临时数据库迁移 → 完整性检查 → 原子替换 → 重建缓存和闹钟
```

恢复失败不得破坏当前数据库。

### 13.3 CSV

- UTF-8 with BOM，兼容常见表格软件。
- 日期使用 ISO 格式，显示文案使用单独列。
- 金额导出币种和最小单位换算后的十进制字符串。
- 导出统计必须调用同一 Domain Engine，不重新实现公式。

---

## 14. 安全与隐私

### 14.1 数据分类

| 级别 | 数据 | 处理要求 |
|---|---|---|
| 高敏感 | API Key、自定义鉴权头 | Keychain/Keystore，禁止日志和备份 |
| 敏感 | 工资、精确打卡、备注、对话 | 本地默认，出站需授权和最小化 |
| 个人配置 | 排班、闹钟、性格 | 本地保存，云同步需用户开启 |
| 非敏感 | 颜色、语言、主题 | 普通配置存储 |

### 14.2 日志脱敏

日志拦截器必须移除：

- `Authorization`、`api-key`、自定义密钥头。
- 请求和响应正文中的密钥格式。
- 工资金额。
- 精确打卡时间。
- 用户备注和完整对话。
- 文件路径中的用户标识。

### 14.3 剪贴板和截图

- 密钥输入框默认掩码。
- 提供显示/隐藏，不默认复制。
- Android 敏感设置页和 iOS 应用切后台快照可使用内容遮罩。
- 用户主动复制密钥后提示剪贴板风险，但不得私自清空其他剪贴板内容。

### 14.4 数据删除

分别提供：

- 清除对话。
- 清除 AI 操作历史。
- 清除模型配置和安全存储凭据。
- 清除工资和考勤。
- 清除全部 App 数据。

每类操作说明可恢复性并二次确认。

---

## 15. 错误模型与可靠性

### 15.1 统一错误

```dart
sealed class AppFailure {
  ValidationFailure
  PermissionFailure
  NetworkFailure
  AuthenticationFailure
  RateLimitFailure
  StorageFailure
  ConflictFailure
  PlatformFailure
  ProviderProtocolFailure
  UnknownFailure
}
```

每个 Failure 包含：

- 稳定错误码。
- 用户可读信息键。
- 是否可重试。
- 是否需要设置入口。
- 内部诊断 ID。

### 15.2 事务边界

- 排班规则 + 缓存失效：同一事务。
- 打卡记录写入：单事务。
- 工资结算 + 快照：同一事务。
- AI 业务修改 + action 状态：同一事务。
- 平台闹钟注册不属于 SQLite 事务，通过同步状态和重试补偿。

### 15.3 幂等

- Alarm：稳定平台 ID + payload hash。
- AI Action：一次性 token + idempotency key。
- 节假日更新：region + year + version。
- 备份恢复：backup ID + checksum。

---

## 16. 性能设计

| 场景 | 目标 | 方法 |
|---|---:|---|
| 冷启动可操作 | ≤ 2.5 秒 | 延迟初始化 AI 和非关键服务 |
| 月历查询 | ≤ 300 ms | 日期缓存、范围查询、避免逐日 DB 请求 |
| 一年统计 | ≤ 1 秒 | 批量查询、纯函数计算、必要时聚合缓存 |
| 三年排班生成 | ≤ 2 秒 | 后台 Isolate/线程计算、批量事务写入 |
| 首页数据库查询 | ≤ 100 ms 目标 | 单次聚合查询或并行独立查询 |
| AI 首字响应 | 取决于服务商 | 流式输出、取消支持、状态提示 |

禁止在 UI 主线程执行：

- 三个月以上排班批量重算。
- 大型 CSV 导出。
- 备份压缩和恢复校验。
- 一年以上统计重建。

---

## 17. 测试设计

### 17.1 单元测试

核心 Domain 覆盖率目标不低于 90%：

- 排班优先级。
- 大小周锚点和跨年。
- 自定义周期负 offset。
- 跨夜、跨月、闰年和时区。
- 多段出勤和重叠校验。
- 工时取整。
- 时薪、日薪、月薪和三类加班。
- 工资结算快照稳定性。
- AI Tool Schema 和确认 token。
- Alarm diff 和幂等。

### 17.2 数据库测试

- 每个 migration 从上一版本升级。
- 空库创建。
- 外键和唯一约束。
- 事务回滚。
- 恢复备份后完整性。
- 缓存可删除重建且结果一致。

### 17.3 Widget 测试

- 月历颜色、文字、今天和选中态。
- 200% 字体缩放。
- 深色模式。
- 空、错、权限和加载状态。
- AI 统计卡和操作确认卡。
- API Key 掩码与显示切换。

### 17.4 集成测试

1. 首次配置大小周 → 日历 → 闹钟。
2. 打卡 → 工时 → 工资。
3. 夜班跨日 → 统计 → 结算。
4. 节假日更新不覆盖手工修改。
5. AI 提案 → 取消，不产生业务写入。
6. AI 提案 → 确认 → 联动更新 → 撤销。
7. 备份 → 清空 → 恢复 → 闹钟重建。

### 17.5 原生真机测试

Android：

- 精确闹钟权限允许/拒绝/撤销。
- 重启恢复。
- 省电模式。
- 厂商后台限制。
- 锁屏响铃和通知渠道。

iOS：

- 通知允许/拒绝/撤销。
- App 前台、后台和被终止。
- 静音和专注模式说明。
- 待处理通知窗口补充。

### 17.6 AI 安全测试

- Prompt injection 要求跳过确认。
- 模型伪造工具成功结果。
- 无效/缺字段 JSON。
- 重复工具调用。
- 过期 token。
- 预览后数据变化。
- 请求模型读取 API Key。
- 关闭工资权限后询问工资。
- 流式中断和取消。

---

## 18. 可观测性

### 18.1 匿名事件

允许记录：

```text
onboarding_completed
schedule_rule_created
day_override_saved
alarm_permission_result
alarm_sync_result
attendance_record_saved
payroll_viewed
ai_connection_test_result
ai_tool_proposed
ai_tool_confirmed
ai_tool_result
data_exported
```

不得上报：

- 具体日期。
- 工资金额。
- 打卡时间。
- 备注和对话正文。
- API 地址中的私有路径。
- API Key 或鉴权头。

### 18.2 诊断

- 每次异常生成本地诊断 ID。
- 用户主动反馈时可选择导出脱敏诊断包。
- Alarm Sync 和 AI Action 保留本地状态码，方便定位而不暴露内容。

---

## 19. CI/CD 与发布

### 19.1 CI 流程

每个合并请求至少执行：

```text
format check
static analysis
unit tests
database migration tests
widget smoke tests
Android debug build
iOS build validation（macOS runner）
dependency license/security scan
```

### 19.2 构建环境

- 提交依赖锁文件。
- CI 使用固定 Flutter SDK 和 JDK/Xcode 版本。
- 签名证书、商店密钥和服务端令牌进入 CI Secret，不写入仓库。
- 开发、测试、生产环境使用独立配置。
- Release 默认关闭详细网络正文日志。

### 19.3 发布前检查

- Android/iOS 权限描述与实际使用一致。
- 隐私清单包含 AI 数据出站说明。
- 精确闹钟用途与产品核心能力一致。
- API Key 不进入崩溃报告和备份。
- 节假日数据版本可查看。
- 工资页面展示“个人预估”说明。
- 无账号情况下数据删除入口完整。

---

## 20. 开发阶段划分

### 阶段 0：工程骨架

- Flutter 项目、主题、路由、状态管理。
- SQLite、迁移、日志、错误模型。
- Android/iOS CI 构建。

完成标准：两端可安装，数据库可迁移，基础导航与原型一致。

### 阶段 1：排班闭环

- 班次模板。
- 单休、双休、大小周和自定义周期。
- 节假日数据。
- 月历、日期详情、单日/批量覆盖。

完成标准：排班引擎单测通过，未来 24 个月结果稳定。

### 阶段 2：闹钟

- 原生桥接。
- 权限和能力状态。
- 排班联动、重启恢复和自检。

完成标准：Android/iOS 真机核心用例通过，重复同步无重复闹钟。

### 阶段 3：出勤、统计和工资

- 打卡和补录。
- 工时引擎。
- 三种工资模式。
- 统计图表、结算和 CSV。

完成标准：UI、导出和测试夹具计算结果一致。

### 阶段 4：AI 助理

- Provider 配置与安全存储。
- 对话和流式响应。
- 功能知识。
- 查询工具。
- 提案、确认、执行和撤销。
- 性格与虚拟人。

完成标准：AI 不可绕过工具网关；安全测试和端到端操作通过。

### 阶段 5：备份与发布质量

- 备份恢复。
- 深色模式、无障碍和性能。
- 隐私、埋点、崩溃和发布检查。

---

## 21. 编码规范

### 21.1 通用

- 代码、类型和数据库字段使用英文；用户文案进入国际化资源。
- 一个文件只承担清晰单一职责。
- 不在 Widget 中写业务计算。
- 不捕获异常后静默忽略。
- 不使用魔法数字表示分钟、金额、状态和权限。
- 所有时间通过注入的 `Clock` 获取，测试禁止依赖真实当前时间。
- 所有 ID 使用 UUID/ULID 类稳定 ID，不使用列表下标。

### 21.2 Domain

- 纯函数优先。
- 不可变对象优先。
- 构造时校验不变量。
- 用 `Result/Either` 或明确异常边界表达可预期失败。
- Repository 返回领域对象，不返回数据库 Row。

### 21.3 UI

- 使用设计令牌，不散落颜色和间距常量。
- 触控区域满足平台要求。
- 所有图标按钮有语义标签。
- 金额、日期和工时格式集中处理。
- 异步按钮防重复提交。

### 21.4 Review 阻断项

以下情况不得合并：

- 明文 API Key。
- AI 直接写 Repository/Database。
- 新工资公式无单元测试。
- 新排班规则无跨年和负 offset 测试。
- 闹钟注册无稳定 ID 或幂等处理。
- 数据库变更无 migration。
- Release 日志包含敏感正文。

---

## 22. Definition of Done

单项功能完成需要同时满足：

1. 对应 `功能说明文档.md` 需求编号验收通过。
2. 领域逻辑单元测试通过。
3. 数据库变更包含迁移和迁移测试。
4. Android/iOS 目标平台完成验证。
5. 加载、空、失败、无权限和离线状态完整。
6. 深色模式、大字体和无障碍标签检查通过。
7. 无新增敏感日志或不必要权限。
8. 计算结果有 trace，写操作可审计。
9. 文档和实际实现同步更新。
10. CI 全部通过。

---

## 23. 新窗口开发启动清单

在新的开发任务中按以下顺序执行：

1. 读取 `功能说明文档.md` 和本文件全文。
2. 检查当前目录，确认是否已有 Flutter 工程；当前 HTML 原型不是工程骨架。
3. 若尚无 Flutter 工程，先完成阶段 0，不直接从 AI 或闹钟模块开始。
4. 创建需求追踪表，将 `F-ONB`、`F-SCH`、`F-HOL`、`F-HOME`、`F-CAL`、`F-ATT`、`F-WAGE`、`F-ALM`、`F-AI` 映射到实现和测试。
5. 优先实现排班领域引擎和单元测试，再实现页面。
6. 闹钟必须在 Android/iOS 真机验证，模拟器结果不能作为最终验收。
7. 工资计算只实现可配置规则，不硬编码法律结论。
8. AI 模块必须最后通过 ToolGateway 接入，不允许模型直接操作数据库。
9. 每完成一个阶段，更新文档中的实现状态、偏差和未决项。

建议新窗口的首个开发指令：

```text
请完整阅读《功能说明文档.md》和《开发设计文档.md》，检查当前工作区，
按照“阶段 0：工程骨架”开始建立 Flutter Android/iOS 项目。
先列出即将创建的目录、依赖和验收命令，再实现并运行测试；
不得跳过文档规定的分层、安全存储和数据库迁移要求。
```

---

## 24. 已知未决项

以下事项不阻塞阶段 0～1，进入相关阶段前确认：

- 正式产品名称、Logo 和商店文案。
- 首发支持的最低 Android/iOS 版本是否需要调整。
- 节假日数据的正式来源和服务部署位置。
- 默认预置工资规则是否只提供空模板。
- AI 自定义 REST Provider 的高级字段映射范围。
- 是否在 V1.0 上线自动本地备份，或延后至 V1.1。
- 是否需要登录和云同步；当前设计默认不需要。
