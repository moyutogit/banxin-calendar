# 排班日历、智能闹钟与工资统计 App 开发规范需求设计

## 0. 文档信息

| 项目 | 内容 |
|---|---|
| 产品暂定名 | 班薪日历 |
| 文档版本 | V1.0 |
| 产品形态 | Android / iOS 手机 App |
| 核心定位 | 记录单双休、节假日调休和自定义班次，并联动闹钟、出勤工时及工资预估 |
| 默认地区与币种 | 中国大陆、人民币（可在设置中修改） |
| 数据策略 | 本地优先、离线可用；登录与云同步作为可选能力 |

> 本产品中的工资金额为个人记账和预估结果，不替代企业工资条或法定薪资核算。

---

## 1. 产品目标

### 1.1 用户问题

1. 单休、双休、大小周和节假日补班规则复杂，普通日历无法准确展示个人工作安排。
2. 班次变化后需要逐个开关闹钟，容易在休息日误响或工作日漏响。
3. 用户难以随时知道本月已工作多久、加班多久，以及预计能拿到多少工资。

### 1.2 核心目标

1. 通过月历清晰展示每天的班次、休息和调休状态。
2. 根据排班自动生成、启停 App 内闹钟。
3. 记录计划与实际出勤，自动统计工时、加班和工资。
4. 当用户临时改班、请假或调整节假日安排时，日历、闹钟、工时和工资同步更新。

### 1.3 成功指标

| 指标 | 目标 |
|---|---:|
| 首次完成排班配置率 | ≥ 70% |
| 月历打开后 3 秒内识别今日班次的用户比例 | ≥ 90% |
| 排班变更后闹钟同步成功率 | ≥ 99.9% |
| 月度工时计算正确率 | 100%（基于已录入数据和规则） |
| 关键页面崩溃率 | < 0.1% |

---

## 2. 用户与典型场景

### 2.1 目标用户

- 实行单休、双休、大小周或轮班制的上班族。
- 节假日经常补班、换休的服务业、制造业和项目制工作人员。
- 需要自行核对工时、加班和工资的小时工、日薪或月薪用户。

### 2.2 典型场景

1. 用户设置“本周双休、下周单休”的大小周规则，App 自动铺满未来日历。
2. 国家法定节假日发布后，App 标记放假和补班日期；用户可按单位安排再次修改。
3. 工作日早上 07:00 响铃，休息日自动停用；夜班则使用另一套闹钟。
4. 用户下班后补录上下班时间，App 自动扣除休息时长并更新工资预估。
5. 用户月底查看正常工时、加班、请假、应出勤天数和预计工资。

---

## 3. 范围定义

### 3.1 V1.0 必须实现

- 单休、双休、大小周、自定义周期排班。
- 早班、白班、晚班、夜班、休息及用户自定义班次。
- 中国大陆法定节假日与调休数据导入、更新及手工覆盖。
- 月历颜色标记、日期详情和批量改班。
- App 内闹钟创建、响铃、稍后提醒、停止及按排班自动启停。
- 手工打卡或补录上下班时间。
- 计划工时、实际工时、正常工时和加班工时统计。
- 时薪、日薪、月薪三种工资预估模式。
- 月度统计、记录导出和本地备份。
- AI 助理对话、软件功能问答、数据总结和设置操作。
- 支持配置 OpenAI 兼容或自定义大模型 API 地址、密钥和模型名称。
- 支持设置 AI 助理名称、性格、表达风格和虚拟人形象。

### 3.2 V1.0 不包含

- 修改手机系统“时钟”App 中已有的闹钟。
- 企业考勤机、门禁或 HR 系统的强制集成。
- 自动发放工资、个税申报或具备法律效力的工资核算。
- 强制 GPS 定位、人脸识别或后台录音。
- 多人排班、主管审批和企业组织架构；可作为后续企业版能力。

---

## 4. 术语与状态

| 术语 | 定义 |
|---|---|
| 班次 | 一段带有开始时间、结束时间、休息时长、颜色和默认闹钟的工作模板 |
| 排班规则 | 按星期或周期批量生成工作日、休息日和班次的规则 |
| 单日覆盖 | 用户仅针对某一天设置的班次、休息、请假或加班记录 |
| 调休上班 | 原本属于周末或休息日，但因节假日安排需要上班 |
| 法定节假日 | 节假日数据源标记的放假日期 |
| 计划工时 | 根据排班班次计算出的应工作时长 |
| 实际工时 | 根据实际上下班记录并扣除休息后得到的时长 |
| 计薪工时 | 依据工资规则最终参与工资计算的时长 |
| 工资预估 | 根据用户输入规则计算的个人参考金额 |

### 4.1 日期主状态

每个自然日只能有一个“主状态”，并可附加异常标签。

| 主状态 | 说明 | 是否默认生成闹钟 |
|---|---|---|
| 工作 | 正常排班工作日 | 是 |
| 调休上班 | 节假日安排产生的补班日 | 是 |
| 休息 | 周休或排班轮休日 | 否 |
| 法定节假日 | 法定放假日 | 否 |
| 请假 | 事假、病假、年假等 | 按请假时段决定 |

可附加标签包括：迟到、早退、缺卡、旷工、加班、换班、出差、手工修改。

---

## 5. 核心业务规则

### 5.1 排班规则

#### 5.1.1 支持的排班模式

| 模式 | 配置方式 | 示例 |
|---|---|---|
| 双休 | 设置每周工作日和休息日 | 周一至周五白班，周六、周日休息 |
| 单休 | 设置每周唯一或多个固定休息日 | 周一至周六白班，周日休息 |
| 大小周 | 选择锚点周及两周模板 | A 周双休，B 周仅周日休息，循环执行 |
| 自定义周期 | 设置 1～31 天循环模板 | 白班、白班、夜班、夜班、休、休 |
| 临时排班 | 批量或逐日覆盖 | 本月 10～12 日改夜班 |

#### 5.1.2 大小周锚点

- 用户必须指定一个锚点周，并说明该周是“双休周”还是“单休周”。
- 系统以周一 00:00 至周日 23:59 为一周，允许用户改为周日作为每周首日，但内部计算仍保存明确日期范围。
- 修改锚点时，系统展示受影响的未来日期预览，用户确认后再覆盖。

#### 5.1.3 日期判定优先级

同一天命中多条规则时，按以下优先级确定最终状态：

1. 用户单日覆盖或批量手工修改。
2. 用户自定义的公司节假日、补班或换休安排。
3. 已导入的官方节假日及调休数据。
4. 大小周、自定义周期、单休或双休规则。
5. 默认工作与休息设置。

所有覆盖操作必须保留来源和修改时间，并支持“恢复为规则生成结果”。

#### 5.1.4 班次模板

每个班次包含：

- 班次名称，1～12 个字符。
- 开始时间、结束时间；支持跨自然日，如 20:00～次日 08:00。
- 默认休息分钟数，可为 0～480 分钟。
- 计划计薪时长，可选择自动计算或手工指定。
- 班次颜色和文字简称。
- 关联闹钟模板，可关联 0～5 个。
- 是否计为工作日。

系统预置白班、早班、晚班、夜班和休息模板，均允许修改。

#### 5.1.5 节假日数据

- 按年份下载节假日数据，至少包含日期、名称、放假/补班属性和数据版本。
- 年度数据更新后，只刷新未被用户手工覆盖的日期。
- 网络不可用时继续使用本地已缓存数据。
- 对尚未发布安排的年份显示“以排班规则推算，未包含官方调休”的提示。
- 用户可关闭官方节假日规则，完全按单位日历安排。

### 5.2 出勤与工时

#### 5.2.1 记录方式

- 首页提供“上班打卡”和“下班打卡”。
- 日期详情允许补录、修改、删除打卡时间，并要求保存修改原因（可选预设项）。
- 一天允许存在多段工作记录，以支持中途离岗或拆分班次。
- 夜班记录归属班次开始日，跨月夜班也按开始日归属；统计页可切换为“按自然日拆分”。

#### 5.2.2 工时公式

```text
单段实际工时 = 下班时间 - 上班时间 - 不计薪休息时长
当日实际工时 = 当日所有有效工作段实际工时之和
正常工时 = min(当日实际工时, 当日规则规定的正常工时)
加班工时 = max(0, 当日实际工时 - 当日规则规定的正常工时)
```

- 缺少上班或下班记录时，当日标记为“缺卡”，不自动计入实际工时。
- 时间精度保存到分钟；统计显示支持 0.1 小时或“小时+分钟”。
- 默认不对工时做 15/30 分钟取整；如启用取整规则，必须由用户配置并在明细中显示原始值。
- 计划工时与实际工时分别统计，不得用计划工时冒充实际出勤。

#### 5.2.3 出勤异常

- 迟到：首次有效上班时间晚于计划开始时间加容差。
- 早退：最后有效下班时间早于计划结束时间减容差。
- 加班：实际工时或有效工作时段超过正常工时规则。
- 请假：支持按整天或时间段记录，并区分带薪与不带薪。
- 容差、最小加班时长和加班取整规则均可配置。

### 5.3 工资计算

#### 5.3.1 工资模式

| 模式 | 基本计算口径 |
|---|---|
| 时薪 | 计薪正常工时 × 正常时薪 + 各类加班工时 × 对应时薪 |
| 日薪 | 有效出勤天数 × 日薪 + 加班工资 + 补贴 - 扣款 |
| 月薪 | 月基本工资 + 加班工资 + 补贴 - 请假/缺勤扣款 - 其他扣款 |

#### 5.3.2 可配置字段

- 工资模式、币种、计薪周期起止日。
- 正常时薪、日薪或月基本工资。
- 工作日、休息日、法定节假日加班倍率或固定金额。
- 餐补、夜班补贴、交通补贴、全勤奖等固定或按次补贴。
- 迟到、早退、事假、旷工等扣款规则。
- 是否仅计算已确认出勤记录。
- 金额保留 2 位小数，内部计算至少使用 4 位小数，汇总后统一四舍五入。

#### 5.3.3 月度工资状态

- “预估中”：当前计薪周期尚未结束。
- “待确认”：周期已结束，但用户尚未确认。
- “已结算”：用户已确认最终金额；后续修改出勤时必须提示重新计算。

工资详情必须展示计算过程，不得只展示一个总金额。最低明细为：基本工资、正常工时工资、三类加班工资、补贴、扣款、预估合计、用户确认实发金额和差额。

### 5.4 智能闹钟

#### 5.4.1 产品边界

- App 管理自己创建的闹钟，不承诺读取或修改系统“时钟”App 中的闹钟。
- Android 使用系统精确闹钟能力实现；iOS 使用本地通知实现提醒，并在界面中明确平台能力差异。

#### 5.4.2 闹钟模板

每个模板包含：名称、响铃时间或相对班次时间、重复规则、铃声、音量渐强、振动、稍后提醒时长、最大稍后次数和启用状态。

支持两种时间方式：

1. 固定时间：例如工作日 07:00。
2. 相对时间：例如班次开始前 90 分钟；班次调整后自动重算。

#### 5.4.3 联动规则

- 日期最终状态为工作或调休上班时，启用该班次关联闹钟。
- 日期最终状态为休息、法定节假日或整日请假时，关闭该日排班闹钟。
- 用户改单日班次、请假或恢复规则后，必须立即重算未来闹钟。
- 在闹钟触发前 24 小时内发生变化时，向用户显示“明日闹钟已调整”的提示。
- 用户可以锁定某个单次闹钟；锁定后排班变化不得自动删除，界面需标记“已锁定”。
- 跨夜班次的“上班前闹钟”按班次实际开始日期与时间计算。

#### 5.4.4 异常与恢复

- 未获得通知或精确闹钟权限时，首页和闹钟页持续展示可操作的风险提示。
- 设备重启、时区或系统时间变化后，重新校验并注册未来闹钟。
- 仅预注册合理时间窗口内的闹钟，建议滚动维护未来 30 天，并每日刷新。
- 用户关闭权限后，保留闹钟配置但将状态标记为“无法响铃”。
- 系统勿扰、省电或后台限制可能影响提醒时，提供对应设置指引。

### 5.5 AI 助理

#### 5.5.1 能力范围

AI 助理必须完整了解 App 已上线功能、字段含义和业务规则，并提供以下能力：

1. 自由对话：回答排班、出勤、工时、工资和闹钟相关问题，也支持普通陪伴式聊天。
2. 功能指导：说明某项功能在哪里、如何配置、当前权限是否正常。
3. 软件操作：通过自然语言创建或修改工作日、休息日、大小周、班次、请假、闹钟和工资规则。
4. 数据查询：回答“本月工作了几天”“哪天加班最多”“预计工资多少”等问题。
5. 总结建议：生成周报、月报、异常提醒和可执行建议。
6. 个性化：设置助理名称、性格、语气、回复长度和虚拟人形象。

AI 生成的统计数字不得自行估算。排班、工时和工资数值必须由 App 本地确定性计算引擎提供，AI 只负责理解问题、调用查询工具和组织语言。

#### 5.5.2 对话操作流程

AI 不得直接修改本地数据库。所有写操作必须遵循：

```text
用户表达意图 → AI 生成结构化操作草案 → App 校验规则
→ 展示变更前后预览 → 用户确认 → App 执行 → 返回结果和撤销入口
```

- 查询类操作可直接返回结果，但必须遵守用户设置的数据权限范围。
- 新增、修改和删除排班、闹钟、工资规则等写操作必须逐次确认。
- 批量修改、覆盖未来排班、修改已结算工资等高影响操作必须额外说明影响日期和记录数量。
- 用户确认的是结构化变更内容，而不是 AI 的自然语言描述。
- 执行前由业务模块再次校验日期、冲突、权限和数据版本；校验失败时不得部分写入。
- 执行后生成操作记录，并提供至少一次即时撤销；批量操作建议保留 30 天可恢复记录。
- AI 回复中不得声称“已设置成功”，除非 App 工具返回明确成功结果。

#### 5.5.3 软件工具接口

建议向 AI 暴露受控工具，而不是数据库或任意代码执行权限：

| 工具 | 用途 | 默认权限 |
|---|---|---|
| `get_app_capabilities` | 获取当前版本功能和使用说明 | 允许 |
| `get_schedule` | 查询指定范围的最终排班 | 需开启排班读取 |
| `propose_schedule_change` | 生成排班变更预览 | 需开启排班读取 |
| `apply_schedule_change` | 应用已确认的排班变更 | 每次确认 |
| `get_attendance_summary` | 获取确定性统计结果 | 需开启出勤读取 |
| `get_wage_summary` | 获取工资汇总和计算明细 | 需开启工资读取 |
| `propose_alarm_change` | 生成闹钟变更预览 | 需开启闹钟读取 |
| `apply_alarm_change` | 应用已确认的闹钟变更 | 每次确认 |
| `undo_ai_action` | 撤销最近一次 AI 操作 | 用户主动触发 |

所有工具参数必须使用 JSON Schema 或同等强类型定义；日期使用 `YYYY-MM-DD`，时间使用本地时区的 `HH:mm`，金额和分钟数禁止由自然语言字符串代替。

#### 5.5.4 大模型连接设置

设置页支持：

- 接口类型：OpenAI 兼容接口、自定义 REST API；后续可增加内置服务商模板。
- API Base URL，必须为合法 HTTPS 地址；开发调试环境可单独允许局域网 HTTP。
- API Key，密码形式显示，支持粘贴、显示/隐藏、更新和清除。
- 模型名称、请求超时、最大输出长度和流式响应开关。
- 高级设置：自定义请求头、组织 ID、兼容参数映射；自定义请求头中的敏感值同样进入安全存储。
- “测试连接”按钮只发送最小探测请求，返回连通性、鉴权和模型可用状态，不保存测试对话。
- API 地址、密钥或模型变更后，下一次对话使用新配置；进行中的请求不强制切换。

错误状态至少区分：地址无效、网络失败、TLS 失败、鉴权失败、模型不存在、限流、余额不足、响应格式不兼容和超时。

#### 5.5.5 数据上下文与隐私

- 默认只发送完成当前问题所必需的数据，优先发送本地计算后的汇总，不发送整库记录。
- 用户可分别授权排班、出勤、工资、闹钟和备注数据；工资金额、打卡明细和备注默认关闭。
- 每次请求应能查看“本次向模型发送了哪些类型的数据”，支持一键复制或清空对话。
- 系统提示词包含功能说明、工具定义和安全规则，不得包含 API 密钥。
- 用户在聊天中输入的密钥、身份证号等敏感内容应在本地提示风险，并避免写入分析埋点。
- 模型不可访问定位、通讯录、文件或相册，除非用户针对当前任务明确选择并授权。

#### 5.5.6 性格与虚拟人

- 预置“温柔陪伴、简洁专业、活泼有趣”三种性格，并支持自定义描述。
- 可设置助理名称、称呼用户的方式、回复详细程度、是否主动提醒和表情使用频率。
- 虚拟人支持内置 2D 形象、头像颜色和背景；内置资源随 App 安装，离线可显示。
- 性格仅影响表达方式，不得改变事实、计算结果、确认流程或权限边界。
- V1.0 虚拟人使用静态或轻量动效；语音、口型驱动和 3D 形象作为后续能力。
- 虚拟人形象不得仿冒真实人物或暗示真人客服。

---

## 6. 信息架构与页面规范

### 6.1 底部导航

底部使用 5 个一级入口：

1. 首页
2. 日历
3. AI 助理
4. 统计
5. 我的

AI 助理位于中间突出位置；闹钟从首页快捷卡片和“我的 > 闹钟设置”进入。

### 6.2 首次使用流程

1. 欢迎页：说明“排班、闹钟、工资预估、AI 助理”四项价值。
2. 选择排班模式：双休、单休、大小周、自定义周期。
3. 配置班次：上下班时间、休息时长。
4. 导入当年节假日与调休。
5. 配置工资：可跳过，稍后填写。
6. 配置闹钟并申请通知/闹钟权限；只有用户开启闹钟功能后才申请相关权限。
7. 展示未来 14 天排班预览，用户确认后进入首页。
8. 首次进入 AI 助理时再配置模型接口和数据权限，不阻塞其他核心功能使用。

首次流程支持返回修改和稍后完成；未完成项在首页以任务卡提示。

### 6.3 首页

首页自上而下显示：

- 今日日期、节日/节气可选信息。
- 今日状态卡：班次名称、计划时段、实际出勤状态。
- 下一个闹钟：时间、用途、开关和权限状态。
- 上班/下班打卡主按钮；根据状态只突出当前可执行操作。
- 本月摘要：出勤天数、实际工时、加班工时、预计工资。
- 未来 7 天横向排班条。

若今日无班，主卡显示“今天休息”；若明日有班，同时显示明日首个闹钟。

### 6.4 月历页

#### 6.4.1 布局

- 顶部：月份切换、回到今天、更多操作。
- 中部：标准 7 列月历，每格展示日期、班次简称和异常角标。
- 底部：选中日概要；上滑进入日期详情。
- 提供“月 / 周”视图切换，V1.0 默认月视图。

#### 6.4.2 颜色规范

| 类型 | 建议颜色 | 辅助标识 |
|---|---|---|
| 白班 | `#3B82F6` 蓝色 | “白” |
| 早班 | `#10B981` 绿色 | “早” |
| 晚班 | `#F59E0B` 橙色 | “晚” |
| 夜班 | `#7C3AED` 紫色 | “夜” |
| 休息 | `#E5E7EB` 浅灰 | “休” |
| 法定节假日 | `#EF4444` 红色 | “假” |
| 调休上班 | 班次色 + 橙色描边 | “班”角标 |
| 请假 | `#FDE68A` 淡黄 | “请” |

- 用户可修改班次颜色，但系统应检测颜色是否过于接近。
- 颜色不能作为唯一信息载体，必须同时提供文字、图标或边框。
- 今天使用独立圆形描边；选中日使用高对比外框，不能覆盖班次色。
- 深色模式下使用独立色值并满足文字对比度要求。
- 同一天存在加班、迟到等信息时，用右上角小图标或角标展示，不再增加整格背景色。

#### 6.4.3 交互

- 单击日期：查看概要。
- 双击或点击编辑：改单日班次、休息、请假、加班或打卡记录。
- 长按日期：进入多选，可批量改班、设休息或恢复规则。
- 修改后即时刷新闹钟、统计和工资预估。
- 月历顶部可筛选班次、异常和是否完成出勤记录。

### 6.5 日期详情页

展示并允许编辑：

- 日期主状态及其来源：手工、节假日、排班周期。
- 计划班次、计划工时和关联闹钟。
- 实际上下班记录、休息时长、实际工时。
- 请假、加班、迟到、早退等异常。
- 当日工资明细。
- 备注；最多 500 字。
- “恢复规则结果”入口。

### 6.6 统计页

#### 6.6.1 时间范围

- 本周、本月、上月、自定义日期范围。
- 默认按计薪周期统计，允许切换自然月。

#### 6.6.2 指标

- 应出勤天数、实际出勤天数、休息天数、法定节假日天数。
- 计划工时、实际工时、正常工时、加班工时。
- 迟到次数/分钟、早退次数/分钟、请假天数/小时、缺卡次数。
- 预估工资、补贴、扣款和用户确认实发工资。

#### 6.6.3 图表

- 工时构成：正常工时与三类加班工时的堆叠条形图。
- 每日工时趋势：折线或柱状图。
- 班次分布：各班次天数横向条形图。
- 所有图表下方必须提供数值明细，确保无障碍和可核对性。

### 6.7 AI 助理页

- 顶部展示虚拟人形象、助理名称、在线/未配置状态和设置入口。
- 中部为消息流，区分用户消息、AI 消息、统计卡片和操作确认卡片。
- 操作确认卡必须展示修改对象、日期范围、变更前后、联动影响和确认/取消按钮。
- 底部输入框支持文字输入、快捷问题和后续的图片附件入口。
- 提供“总结本月出勤、设置大小周、预估工资、检查闹钟”等上下文快捷问题。
- 未配置接口时显示引导页，不进入空白聊天页；配置失败时保留用户输入并允许重试。
- 对话页可查看当前模型、清空上下文、开始新对话和查看已执行操作。

### 6.8 我的/设置

- 排班规则管理。
- 班次模板管理。
- 闹钟模板与权限检查。
- 工资规则与计薪周期。
- 节假日数据版本与更新。
- 周起始日、工时显示方式、币种、主题、语言。
- 数据导出、备份、恢复和清空。
- AI 接口、密钥、模型、数据权限、性格和虚拟人设置。
- 隐私政策、问题反馈和版本信息。

---

## 7. 数据模型

### 7.1 核心实体

#### UserSettings

```text
id, locale, timezone, currency, weekStart,
hourDisplayMode, theme, holidayRegion, createdAt, updatedAt
```

#### ShiftTemplate

```text
id, name, shortName, startTime, endTime, crossDay,
unpaidBreakMinutes, plannedPaidMinutes, color,
isWorkday, enabled, createdAt, updatedAt
```

#### ScheduleRule

```text
id, name, type, anchorDate, cycleLengthDays,
cyclePayload, effectiveStart, effectiveEnd, priority, enabled
```

#### CalendarDay

```text
date, resolvedStatus, shiftTemplateId, sourceType,
sourceId, plannedMinutes, isUserOverridden, resolvedAt
```

#### DayOverride

```text
id, date, status, shiftTemplateId, reason, note,
createdAt, updatedAt
```

#### HolidayRecord

```text
date, region, name, dayType, year, dataVersion, updatedAt
```

#### AlarmTemplate / AlarmInstance

```text
AlarmTemplate: id, name, mode, fixedTime, offsetMinutes,
sound, vibrate, snoozeMinutes, maxSnoozeCount, enabled

AlarmInstance: id, templateId, scheduleDate, triggerAt,
shiftId, locked, platformAlarmId, status, lastSyncedAt
```

#### AttendanceRecord

```text
id, workDate, clockInAt, clockOutAt, unpaidBreakMinutes,
source, status, editReason, note, createdAt, updatedAt
```

#### LeaveRecord

```text
id, workDate, leaveType, startAt, endAt,
paid, deductionAmount, note
```

#### WageRule / PayrollPeriod

```text
WageRule: id, mode, baseRate, overtimeRates,
allowanceRules, deductionRules, periodStartDay, roundingRule

PayrollPeriod: id, startDate, endDate, status,
calculatedAmount, confirmedAmount, calculationSnapshot, confirmedAt
```

#### AIProviderConfig

```text
id, providerType, baseUrl, modelName, encryptedCredentialRef,
customHeadersRef, timeoutSeconds, maxOutputTokens, streamEnabled,
connectionStatus, lastTestedAt, createdAt, updatedAt
```

`encryptedCredentialRef` 只保存系统安全存储中的引用，不得在 SQLite、日志或普通备份中保存明文密钥。

#### AssistantPersona

```text
id, displayName, presetType, customInstruction,
replyLength, initiativeLevel, emojiLevel, avatarAssetId,
voiceEnabled, updatedAt
```

#### Conversation / Message

```text
Conversation: id, title, modelSnapshot, createdAt, updatedAt, archivedAt

Message: id, conversationId, role, content, contentType,
toolCallId, localOnly, createdAt
```

#### AIAction

```text
id, conversationId, actionType, toolName, proposedPayload,
validatedPayload, beforeSnapshot, afterSnapshot,
status, confirmationToken, confirmedAt, executedAt, undoneAt, errorCode
```

### 7.2 数据一致性

- 日历解析结果可以缓存，但排班规则或节假日版本变化后必须失效并重算。
- 工资结算必须保存规则和明细快照，避免后续改工资规则影响历史已结算月份。
- 闹钟实例由最终日历状态派生，不得单独成为排班事实来源。
- 删除班次模板前，必须提示其被多少规则和历史记录引用；历史记录保留模板快照。
- 所有时间按带时区的时间戳保存，班次日期单独保存本地自然日。
- AIAction 的确认令牌必须与当前预览内容、用户和数据版本绑定，内容变化后原确认立即失效。
- 删除对话不等于删除已执行的业务操作；业务数据和 AI 操作审计记录按各自生命周期管理。

---

## 8. 技术实现约束

### 8.1 客户端架构

- 推荐采用原生能力可靠的跨平台方案；排班、统计可共享代码，闹钟、通知和系统权限必须使用平台原生实现并分别测试。
- 本地使用 SQLite 或等价事务数据库，确保无网时可查、可记、可响铃。
- 排班解析、工时计算和工资计算应拆为独立纯函数模块，便于单元测试。
- 节假日服务只提供版本化数据；用户个人工资、出勤和闹钟默认不上传。
- 若启用云同步，应采用账号级加密传输、冲突检测和软删除机制。

### 8.2 模块边界

```text
排班规则 + 节假日 + 单日覆盖
              ↓
         日历解析引擎
          ↙         ↘
   闹钟调度器       工时统计器
                       ↓
                   工资计算器

AI 对话层 → 受控工具网关 → 上述业务模块
                ↓
          预览 / 确认 / 审计 / 撤销
```

- 日历解析引擎输出是闹钟与统计的共同输入。
- 闹钟调度失败不能阻断排班保存，但必须向用户显示失败状态。
- 工资计算错误不能影响原始考勤记录，计算结果可安全重建。
- AI 对话层不得越过受控工具网关访问数据库；工具网关复用现有业务模块的校验和事务。

### 8.3 平台适配

#### Android

- 精确闹钟需要使用系统闹钟能力，并在使用前检查对应权限。
- 获得设备重启广播后重新注册未来闹钟。
- 适配通知渠道、锁屏展示、全屏响铃页面和厂商后台限制。
- 权限被撤销时取消失效状态并提示重新授权，不得静默假装可用。

#### iOS

- 使用本地通知安排提醒，并申请通知权限。
- 不承诺控制系统“时钟”App，也不承诺在静音、专注模式等所有系统状态下达到系统闹钟相同效果。
- 待处理通知数量接近平台限制时，只安排最近一段时间的提醒，并在 App 启动或后台刷新机会出现时补充。

### 8.4 节假日数据接口建议

```http
GET /api/v1/holidays?region=CN&year=2027
If-None-Match: <etag>
```

响应至少包含：

```json
{
  "region": "CN",
  "year": 2027,
  "version": "2027.1",
  "publishedAt": "2026-12-01T00:00:00+08:00",
  "days": [
    {"date": "2027-01-01", "name": "元旦", "type": "holiday"},
    {"date": "2027-01-04", "name": "元旦调休", "type": "adjusted_workday"}
  ]
}
```

服务端必须支持 ETag 或版本号，客户端更新前展示变更数量，不覆盖用户手工修改。

### 8.5 AI 连接与工具执行架构

- 大模型请求层只负责协议适配、流式响应、重试和错误映射，不承担业务计算。
- 功能知识采用版本化的本地能力清单和帮助文档摘要；App 版本升级后同步更新，不依赖模型记忆猜测功能。
- 对话管理器根据当前问题选择最小上下文，并对出站数据进行权限过滤和敏感字段脱敏。
- 模型返回的工具调用必须先通过 JSON Schema 校验、字段白名单、权限检查和业务预校验。
- 工具调用使用客户端生成的幂等键，网络重试不得造成同一排班或闹钟变更执行两次。
- 确认令牌只允许使用一次并设置短时有效期；App 从后台恢复后，应重新检查变更预览是否过期。
- 模型输出中的文本、Markdown 和链接均按不可信内容处理；禁止执行其生成的代码、SQL、脚本或任意 URL 请求。
- 自定义 API 地址应阻止非预期协议、带凭据 URL 和明显无效地址；正式版仅允许 HTTPS，并明确提示自签名证书风险。
- AI 请求失败不得影响排班、考勤、闹钟和统计等非 AI 功能。

---

## 9. 权限与隐私规范

| 权限/能力 | 申请时机 | 拒绝后的处理 |
|---|---|---|
| 通知 | 用户首次开启闹钟时 | 保留配置，标记“无法提醒”并提供设置入口 |
| 精确闹钟（Android） | 用户启用精确响铃时 | 说明影响，允许改为普通通知提醒 |
| 开机后恢复（Android） | 随安装声明，运行时不打扰用户 | 重启后重建闹钟 |
| 日历访问 | V1.0 默认不申请 | 后续做系统日历同步时再按需申请 |
| 定位 | 不申请 | 核心功能不依赖定位 |
| AI 数据范围 | 首次使用相应数据能力时 | 仅提供普通聊天和功能说明，不发送该类本地数据 |

- 工资、出勤和备注属于敏感个人数据，应默认只保存在设备本地。
- 导出文件前提示其包含的字段；分享行为必须由用户主动发起。
- 支持 App 内 PIN 或生物识别锁，可作为 V1.1。
- 清空数据前二次确认，并明确本地备份是否可恢复。
- 崩溃日志和埋点不得上传工资金额、备注正文或精确打卡时间等敏感字段。
- API Key 必须使用 iOS Keychain、Android Keystore 支持的加密存储或等价安全方案，禁止写入数据库、明文配置、剪贴板日志或云备份。
- API Key 只发送给用户设置的模型 API 地址；测试连接、遥测和崩溃上报不得携带密钥。
- App 应在 API 地址变化时明确提示密钥将发送至新的域名，并要求用户再次确认。
- 对话记录默认保存在本地；是否随模型请求发送历史消息由用户设置和上下文需要共同决定。
- 用户可单独清除 API 配置、对话记录、AI 操作历史和虚拟人偏好。

---

## 10. 非功能要求

### 10.1 性能

- 冷启动至首页可操作时间：主流设备 ≤ 2.5 秒。
- 月历切换和日期解析：普通月份 ≤ 300 毫秒。
- 单次批量生成 3 年排班：≤ 2 秒，期间提供进度状态。
- 统计 1 年出勤记录：≤ 1 秒。

### 10.2 可靠性

- 排班、考勤和工资配置使用事务写入，异常退出不得产生半条记录。
- 闹钟注册状态每天自检一次，App 启动时额外自检。
- 本地自动备份至少保留最近 7 个版本，用户可关闭。
- 日期、跨夜、跨月、闰年、时区切换必须有专项自动化测试。

### 10.3 可用性与无障碍

- 核心文字支持系统字体放大至 200%，不截断关键金额和时间。
- 可点击区域不小于 44×44 pt（iOS）或 48×48 dp（Android）。
- 状态不只依赖颜色表达，并支持屏幕阅读器朗读完整日期和班次。
- 删除、覆盖未来排班、修改已结算月份等高影响操作必须确认。

### 10.4 兼容与本地化

- 中文简体为首发语言。
- 默认 24 小时制，同时支持 12 小时制。
- 默认时区 Asia/Shanghai；用户跨时区时询问是保持原工作地时间还是跟随本地时间。
- 货币格式、每周起始日、节假日区域均与用户设置绑定。

---

## 11. 埋点与日志

建议记录匿名事件，不记录敏感明细：

- `onboarding_completed`：完成首次配置，属性为排班模式。
- `schedule_rule_created`：创建规则，属性为规则类型。
- `day_override_saved`：改单日状态，不上传具体日期和备注。
- `alarm_permission_result`：权限同意/拒绝。
- `alarm_sync_result`：注册成功/失败及非敏感错误码。
- `attendance_record_saved`：新增/修改记录，不上传具体时间。
- `payroll_viewed`：查看统计，金额不上报。
- `data_exported`：导出类型，不上传文件内容。

客户端日志必须对工资、备注、账号和精确时间做脱敏。

---

## 12. 验收标准

### 12.1 排班与日历

1. 给定锚点周为双休周，系统能正确生成至少未来 12 个月的大小周日历。
2. 官方补班日期能覆盖原周末休息规则，并显示“班”角标。
3. 用户将补班日手工改为休息后，后续更新节假日数据不会覆盖该修改。
4. 夜班 20:00～次日 08:00 能正确显示在开始日，计划工时扣除休息后准确。
5. 班次颜色、简称和异常角标在浅色、深色模式下均可识别。

### 12.2 闹钟

1. 工作日生成班次关联闹钟，休息日和法定节假日不生成。
2. 将明日白班改成夜班后，旧闹钟被取消，新闹钟按夜班模板生成。
3. 将明日设为整日请假后，排班闹钟被关闭；锁定的单次闹钟保留。
4. 权限被拒绝或撤销后，页面明确显示“无法响铃”，不显示虚假正常状态。
5. Android 设备重启后，未来闹钟可重建且不重复。

### 12.3 出勤与工资

1. 09:00 上班、18:00 下班、休息 60 分钟，实际工时应为 8 小时。
2. 缺少下班时间时显示缺卡，实际工时不自动生成。
3. 同一天两段工作时间能正确合计并扣除各自休息时长。
4. 三种工资模式均能展示可核对的计算明细。
5. 已结算月份修改出勤时提示重算，历史结算快照仍可查看。
6. 导出数据与页面统计在相同口径下完全一致。

### 12.4 AI 助理

1. 未配置 API 时，AI 页显示配置引导且不影响排班、闹钟和统计功能。
2. 正确地址、密钥和模型通过测试连接；鉴权失败、模型不存在、限流和超时显示不同错误。
3. API Key 不出现在数据库导出、普通备份、日志、埋点、剪贴板记录和界面截图遮罩之外。
4. 用户说“这周六改上班”时，AI 展示明确日期、班次、联动闹钟和影响范围，不直接执行。
5. 未点击确认、确认令牌过期或预览后数据变化时，不得修改任何业务数据。
6. 确认成功后日历、闹钟、统计同步更新，AI 返回真实工具结果并提供撤销。
7. AI 总结中的天数、工时和工资与确定性统计模块完全一致。
8. 关闭工资数据权限后，AI 不得获取或向模型发送工资金额。
9. 更换性格只影响表达方式，不改变工具参数、统计数字和确认要求。
10. 恶意提示要求“跳过确认、读取密钥或执行代码”时，工具网关拒绝执行。

### 12.5 边界测试

- 2 月 29 日、月底跨夜班、12 月至次年 1 月跨年班次。
- 夏令时地区的不存在时间和重复时间。
- 系统时间手工前拨/后拨、时区变化、权限撤销、设备重启。
- 连续多次改单、批量覆盖后恢复、节假日版本升级。
- 0 分钟休息、跨 24:00、超长班次、无工资规则和负数扣款输入。
- 模型返回无效 JSON、重复工具调用、流式响应中断、超长回复和上下文超限。
- API 地址切换域名、证书异常、离线、密钥撤销、接口限流和模型下线。
- AI 预览后用户在其他页面改班、重复点击确认、确认后立即撤销。

---

## 13. 版本规划

### V1.0：个人排班闭环

- 排班日历、节假日调休、班次颜色。
- App 内智能闹钟。
- 手工考勤、工时统计、工资预估。
- 本地备份及 CSV 导出。
- AI 助理聊天、功能问答、统计总结和经确认的软件设置操作。
- 自定义大模型 API、密钥、模型、性格和内置虚拟人形象。

### V1.1：体验增强

- 组件/小组件、快捷打卡、App 锁。
- 云同步、多设备恢复、统计 PDF。
- 系统日历只读/双向同步。
- 工资条拍照识别与差额核对。

### V2.0：协作与自动化

- 企业排班导入、同事换班、主管审批。
- NFC/蓝牙/Wi-Fi 辅助打卡（需单独隐私评估）。
- 多份工作并行排班和多币种工资。

---

## 14. 开发完成定义（Definition of Done）

每项功能只有同时满足以下条件才可标记完成：

1. 产品验收标准通过。
2. 核心计算逻辑有单元测试，排班解析、跨夜工时和工资计算覆盖率不低于 90%。
3. Android 与 iOS 真机完成通知、权限、重启和时区测试。
4. 空状态、加载、失败、无权限和离线状态均有明确界面。
5. 已完成无障碍标签、深色模式和大字体检查。
6. 隐私清单与实际采集字段一致。
7. 埋点不包含工资、备注或精确考勤等敏感数据。
8. 用户文案明确区分“计划”“实际”“预估”“已结算”。

---

## 15. 默认产品决策

为避免开发阶段口径悬而未决，V1.0 采用以下默认值：

- 每周从周一开始，24 小时制，时区为 Asia/Shanghai。
- 官方节假日调休默认开启，用户单日修改优先级最高。
- 工时精确到分钟，不默认取整。
- 夜班和跨月班次默认归属开始日。
- 闹钟采用 App 内管理，不修改系统“时钟”App。
- 工资为预估值，用户可在周期结束后填写实发金额进行差额核对。
- 月历用“颜色 + 文字简称 + 角标”共同表达状态。
