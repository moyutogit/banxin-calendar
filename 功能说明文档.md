# 班薪日历 App 功能说明文档

## 0. 文档用途

| 项目 | 内容 |
|---|---|
| 文档版本 | V1.0 |
| 更新日期 | 2026-08-05 |
| 产品暂定名 | 班薪日历 |
| 适用平台 | Android、iOS |
| 目标读者 | 产品、UI、开发、测试及后续接手任务的 AI/工程师 |
| 文档性质 | V1.0 功能范围、交互行为和验收口径的基准文档 |

本文件是可独立使用的功能说明。后续开发不需要依赖历史对话。

关联文件：

- `开发设计文档.md`：架构、数据模型、算法、接口和工程规范。
- `app-prototype.html`：可交互 HTML 原型。
- `APP开发规范需求设计.md`：早期完整需求稿，发生冲突时以本文件和 `开发设计文档.md` 为准。

---

## 1. 产品概述

### 1.1 产品定位

班薪日历是一款面向单休、双休、大小周和轮班用户的个人工作管理 App。用户可以通过日历记录工作日、休息日、节假日调休和班次，App 自动联动闹钟、出勤工时及工资预估，并提供能够理解软件功能、查询统计和协助设置的 AI 助理。

### 1.2 核心价值

1. 排班清楚：月历用颜色、文字和角标展示每天的班次与休息状态。
2. 提醒可靠：工作日自动启用闹钟，休息日自动停用。
3. 工资有数：计划工时、实际工时、加班和工资均可核对。
4. 设置简单：用户可直接对 AI 说“下周六改成休息”，确认预览后完成设置。

### 1.3 目标用户

- 实行单休、双休或大小周的上班族。
- 需要早班、白班、晚班、夜班或循环轮班的用户。
- 节假日经常补班、换休的服务业、制造业和项目制工作人员。
- 需要自行核对出勤、加班和工资的时薪、日薪或月薪用户。

### 1.4 产品原则

- 本地优先：无网络时仍可查看和修改排班、打卡、统计及使用闹钟。
- 事实可核对：计划、实际、预估和已结算必须明确区分。
- 操作可恢复：批量改单、AI 设置和已结算数据变更必须保留记录或撤销入口。
- 隐私最小化：工资、考勤、API 密钥和备注默认不上传。
- AI 受控：AI 可以提出设置方案，但未经用户确认不得修改业务数据。

---

## 2. V1.0 功能范围

### 2.1 必须实现

| 模块 | 功能 |
|---|---|
| 排班 | 单休、双休、大小周、1～31 天自定义循环、临时改单和批量改班 |
| 班次 | 早班、白班、晚班、夜班、休息及用户自定义班次 |
| 节假日 | 中国大陆法定节假日、调休补班、数据更新及用户覆盖 |
| 日历 | 月视图、颜色标记、文字简称、异常角标、日期详情和筛选 |
| 闹钟 | App 内闹钟、班次联动、稍后提醒、权限检查和重启恢复 |
| 出勤 | 上下班打卡、补录、多段出勤、跨夜班、迟到、早退、请假和缺卡 |
| 工时 | 计划、实际、正常、加班工时统计 |
| 工资 | 时薪、日薪、月薪、加班倍率、补贴、扣款、预估和结算 |
| AI 助理 | 对话、功能问答、数据总结、经确认的软件设置、API 配置、性格和虚拟人 |
| 数据 | 本地存储、备份恢复、CSV 导出、敏感数据保护 |

### 2.2 V1.0 不包含

- 修改手机系统“时钟”App 中已经存在的闹钟。
- 企业考勤机、门禁、HR、审批流和多人排班。
- 自动发工资、个税申报或具备法律效力的工资核算。
- GPS 强制打卡、人脸识别、后台录音。
- 语音对话、3D 虚拟人、数字人口型驱动。
- 多份工作并行排班、多币种工资和企业组织架构。

---

## 3. 信息架构

### 3.1 底部导航

底部固定 5 个一级入口：

1. 首页
2. 日历
3. AI 助理
4. 统计
5. 我的

AI 助理位于中间并使用突出样式。闹钟、班次和工资规则属于设置能力，不单独占用一级导航。

### 3.2 页面清单

```text
首页
├─ 今日班次
├─ 上下班打卡
├─ 下一个闹钟
├─ 本月收入摘要
└─ 未来 7 天排班

日历
├─ 月历/周历
├─ 日期详情
├─ 单日修改
├─ 批量修改
└─ 排班规则与班次模板

AI 助理
├─ 对话
├─ 快捷问题
├─ 数据总结卡片
├─ 设置变更预览/确认/撤销
└─ AI 接口、性格和虚拟人设置

统计
├─ 出勤统计
├─ 工时统计
├─ 工资明细
└─ 导出

我的
├─ 排班与班次
├─ 智能闹钟
├─ 工资规则
├─ 节假日与调休
├─ AI 助理设置
├─ 备份与导出
└─ 显示、语言、隐私和关于
```

---

## 4. 通用业务定义

### 4.1 日期主状态

每个自然日只能有一个主状态，可同时带多个附加标签。

| 主状态 | 说明 | 默认生成排班闹钟 |
|---|---|---|
| 工作 | 正常排班工作日 | 是 |
| 调休上班 | 周末或原休息日因节假日/单位安排上班 | 是 |
| 休息 | 周休、轮休或用户主动设休 | 否 |
| 法定节假日 | 法定放假日 | 否 |
| 请假 | 整日或部分时段请假 | 按未请假时段决定 |

附加标签包括：迟到、早退、缺卡、旷工、加班、换班、出差、手工修改。

### 4.2 日期规则优先级

同一天命中多条规则时，优先级从高到低为：

1. 用户单日覆盖或批量手工修改。
2. 用户录入的公司放假、补班或换休安排。
3. 已导入的官方节假日及调休数据。
4. 大小周、自定义循环、单休或双休规则。
5. 默认工作与休息设置。

用户手工覆盖后，官方节假日更新不得覆盖该日期。日期详情必须显示最终状态来源，并提供“恢复规则结果”。

### 4.3 时间与日期默认口径

- 默认时区：`Asia/Shanghai`。
- 默认每周从周一开始。
- 默认使用 24 小时制。
- 工时精确到分钟，不默认进行 15/30 分钟取整。
- 跨夜班及跨月班次默认归属班次开始日。
- 金额显示保留 2 位小数，内部计算至少保留 4 位小数。
- 日期、时间、金额和工时不得使用仅供展示的格式参与实际计算。

---

## 5. 首次使用与初始化

### 5.1 首次流程

| 步骤 | 内容 | 是否可跳过 |
|---|---|---|
| 1 | 介绍排班、闹钟、工资和 AI 助理价值 | 否 |
| 2 | 选择双休、单休、大小周或自定义周期 | 否 |
| 3 | 设置默认班次、上下班时间和休息时长 | 否 |
| 4 | 导入当年节假日及调休 | 是 |
| 5 | 设置工资模式和金额 | 是 |
| 6 | 设置闹钟并申请必要权限 | 是 |
| 7 | 预览未来 14 天排班并确认 | 否 |

AI 模型接口不在首次主流程中强制配置。用户第一次进入 AI 助理时再配置，未配置 AI 不影响其他功能。

### 5.2 初始化验收

- `F-ONB-001` 用户完成排班配置后，未来 14 天必须显示可核对预览。
- `F-ONB-002` 用户返回上一步修改班次后，预览即时重算。
- `F-ONB-003` 跳过工资或闹钟后，首页提供非阻塞式完成提示。
- `F-ONB-004` 权限只在用户启用相关功能时申请，不在启动时一次性索取。

---

## 6. 排班与班次

### 6.1 排班模式

| 模式 | 配置内容 | 示例 |
|---|---|---|
| 双休 | 每周工作日、休息日及对应班次 | 周一至周五白班，周末休息 |
| 单休 | 每周工作日、固定休息日 | 周一至周六白班，周日休息 |
| 大小周 | 锚点周、单休周模板、双休周模板 | 本周六上班，下周六休息 |
| 自定义周期 | 1～31 天循环序列和锚点日期 | 白、白、夜、夜、休、休 |
| 临时排班 | 单日或日期范围覆盖 | 8 月 10～12 日改夜班 |

### 6.2 大小周规则

- 用户必须指定一个锚点周，并明确该周是单休周还是双休周。
- 默认一周为周一 00:00 至周日 23:59。
- 修改锚点或模板前展示受影响日期范围和未来排班预览。
- 修改规则不得改变已经手工覆盖的日期，除非用户明确勾选“同时覆盖手工修改”。

### 6.3 班次模板

每个班次包含：

- 名称：1～12 个字符。
- 简称：1～2 个汉字或 1～3 个字符。
- 开始和结束时间。
- 是否跨日。
- 不计薪休息时长：0～480 分钟。
- 计划计薪时长：自动计算或手工指定。
- 班次颜色。
- 是否计为工作日。
- 关联闹钟模板：0～5 个。

系统预置早班、白班、晚班、夜班和休息，用户可修改、复制或新增。

### 6.4 排班操作要求

- `F-SCH-001` 用户可创建、修改、停用和复制排班规则。
- `F-SCH-002` 用户可单击日期查看详情，长按进入多选批量修改。
- `F-SCH-003` 单日修改保存后，日历、闹钟、统计和工资必须使用相同最终结果。
- `F-SCH-004` 批量修改前展示日期数量、原状态和新状态摘要。
- `F-SCH-005` 用户可恢复某天或某段日期为规则计算结果。
- `F-SCH-006` 删除仍被规则引用的班次模板时必须阻止或要求先替换引用。
- `F-SCH-007` 历史记录保存班次快照，修改模板不得改变历史已结算数据。

---

## 7. 节假日与调休

### 7.1 数据能力

- 按年份下载中国大陆节假日数据。
- 数据至少包含日期、节日名称、放假/补班属性、发布时间和版本号。
- 官方安排尚未公布的年份显示“按排班规则推算，未包含官方调休”。
- 用户可关闭官方节假日规则，完全使用公司或个人安排。

### 7.2 更新行为

- `F-HOL-001` 无网络时继续使用最近一次已缓存数据。
- `F-HOL-002` 新版本只更新未被用户手工覆盖的日期。
- `F-HOL-003` 更新后展示新增、删除和变化日期数量。
- `F-HOL-004` 调休上班日使用实际班次颜色，并增加“班”角标。
- `F-HOL-005` 法定节假日默认不生成排班闹钟，但用户锁定的单次闹钟不受影响。

---

## 8. 首页

### 8.1 页面内容

首页自上而下显示：

1. 日期、星期和问候语。
2. 今日班次卡：主状态、班次、计划时段、计划工时和休息时间。
3. 上班/下班打卡主按钮。
4. 下一个闹钟及当前可用状态。
5. 本月预计收入、出勤天数、实际工时和加班工时。
6. 未来 7 天排班条。

### 8.2 行为与状态

- `F-HOME-001` 工作日显示班次和打卡操作；休息日显示“今天休息”。
- `F-HOME-002` 今日无班但明日有班时，显示明日首个闹钟。
- `F-HOME-003` 闹钟权限异常时不得显示“正常”，应显示风险提示和设置入口。
- `F-HOME-004` 工资未配置时显示“设置工资规则”，不显示虚构金额。
- `F-HOME-005` 缺卡时首页显示补录提醒，但不自动生成实际工时。
- `F-HOME-006` 点击未来 7 天日期进入对应日期详情。

---

## 9. 月历与日期详情

### 9.1 月历布局

- 顶部：月份切换、回到今天、筛选和添加排班。
- 中部：标准 7 列月历，默认周一为第一列。
- 单元格：日期、班次简称、主状态颜色和异常角标。
- 底部：选中日期概要和“修改”入口。

### 9.2 颜色规范

| 类型 | 默认颜色 | 文字/角标 |
|---|---|---|
| 白班 | `#3B82F6` | 白 |
| 早班 | `#10B981` | 早 |
| 晚班 | `#F59E0B` | 晚 |
| 夜班 | `#7C3AED` | 夜 |
| 休息 | `#E5E7EB` | 休 |
| 法定节假日 | `#EF4444` | 假 |
| 调休上班 | 班次色 + 橙色描边 | 班 |
| 请假 | `#FDE68A` | 请 |

颜色不得成为唯一信息载体。今天、选中日和主状态使用不同层级的描边，不得互相覆盖。

### 9.3 日期详情

展示并允许编辑：

- 日期主状态及来源。
- 计划班次、计划工时和关联闹钟。
- 实际上下班时间、多段出勤和休息时长。
- 请假、加班、迟到、早退和缺卡。
- 当日工资计算明细。
- 备注，最多 500 字。
- 恢复规则结果。

### 9.4 日历验收

- `F-CAL-001` 月份切换后 300 毫秒目标时间内显示结果。
- `F-CAL-002` 月历切换不得改变选中日期以外的业务数据。
- `F-CAL-003` 夜班显示在班次开始日，日期详情说明结束时间为次日。
- `F-CAL-004` 筛选只影响显示，不改变统计和数据。
- `F-CAL-005` 深色模式和字体放大 200% 时仍可识别班次、日期和异常。

---

## 10. 出勤与工时

### 10.1 记录方式

- 首页提供上班打卡和下班打卡。
- 日期详情支持补录、修改和删除。
- 一天支持多段工作记录。
- 手工修改可选择原因：忘记打卡、设备异常、补录、其他。

### 10.2 计算口径

```text
单段实际工时 = 下班时间 - 上班时间 - 不计薪休息时长
当日实际工时 = 所有有效工作段实际工时之和
正常工时 = min(当日实际工时, 当日正常工时上限)
加班工时 = max(0, 当日实际工时 - 当日正常工时上限)
```

允许用户配置最小加班时长、迟到/早退容差和可选取整规则。页面必须显示原始时长和取整后计薪时长。

### 10.3 异常定义

- 迟到：首次有效上班时间晚于计划开始时间加容差。
- 早退：最后有效下班时间早于计划结束时间减容差。
- 缺卡：缺少有效上班或下班记录。
- 加班：符合用户配置的时间或时长条件。
- 请假：整日或时间段；区分带薪与不带薪。

### 10.4 出勤验收

- `F-ATT-001` 09:00～18:00、休息 60 分钟，实际工时为 8 小时。
- `F-ATT-002` 缺少任一端打卡时标记缺卡，实际工时不自动生成。
- `F-ATT-003` 多段出勤分别校验时间重叠，再合计有效时长。
- `F-ATT-004` 跨夜记录按班次开始日归属，可在统计页切换为自然日拆分。
- `F-ATT-005` 修改已结算周期的出勤时提示工资需要重新计算。

---

## 11. 工资

### 11.1 工资模式

| 模式 | 基本公式 |
|---|---|
| 时薪 | 正常计薪工时 × 正常时薪 + 各类加班工时 × 对应时薪 |
| 日薪 | 有效出勤天数 × 日薪 + 加班工资 + 补贴 - 扣款 |
| 月薪 | 月基本工资 + 加班工资 + 补贴 - 请假/缺勤扣款 - 其他扣款 |

### 11.2 配置项

- 工资模式、币种和计薪周期。
- 正常时薪、日薪或月基本工资。
- 工作日、休息日、法定节假日的加班倍率或固定金额。
- 餐补、夜班补贴、交通补贴、全勤奖等。
- 迟到、早退、事假和旷工扣款。
- 是否仅计算已确认出勤。
- 工时和金额取整方式。

### 11.3 工资状态

| 状态 | 说明 |
|---|---|
| 预估中 | 当前计薪周期尚未结束 |
| 待确认 | 周期结束但用户未确认 |
| 已结算 | 用户确认最终金额并生成规则及明细快照 |

### 11.4 工资验收

- `F-WAGE-001` 工资详情展示基本工资、正常工资、各类加班、补贴、扣款和合计。
- `F-WAGE-002` 不得只展示总金额，用户必须能追溯计算来源。
- `F-WAGE-003` 修改当前工资规则不得改变历史已结算周期。
- `F-WAGE-004` 用户可填写实发金额，并查看与预估金额的差额。
- `F-WAGE-005` 页面、导出和 AI 总结必须使用同一计算结果。
- `F-WAGE-006` 工资结果标记为个人预估，不替代企业工资条或法定核算。

---

## 12. 智能闹钟

### 12.1 产品边界

- App 管理自己创建的闹钟，不读取或修改系统“时钟”App 已有闹钟。
- Android 使用系统精确闹钟能力；权限不足时明确提示。
- iOS 使用本地通知提醒，不承诺在静音、专注模式等所有状态下与系统闹钟相同。

### 12.2 闹钟模板

包含名称、固定时间或相对班次时间、铃声、振动、音量渐强、稍后提醒时长、最大稍后次数和启用状态。

支持：

- 固定时间，例如工作日 07:00。
- 相对时间，例如班次开始前 90 分钟。
- 单次锁定，锁定后不随排班自动删除。

### 12.3 联动规则

- 工作或调休上班：启用班次关联闹钟。
- 休息、法定节假日、整日请假：停用排班闹钟。
- 修改未来班次：取消旧闹钟并创建新闹钟。
- 触发前 24 小时内发生变化：提示“明日闹钟已调整”。
- 设备重启、时区或系统时间变化：重新校验未来闹钟。

### 12.4 闹钟验收

- `F-ALM-001` 工作日生成闹钟，休息日不生成。
- `F-ALM-002` 明日白班改夜班后，旧闹钟取消且新闹钟只生成一次。
- `F-ALM-003` 权限被撤销时状态显示“无法响铃”，不得显示虚假正常。
- `F-ALM-004` Android 重启后重建未来闹钟，不产生重复实例。
- `F-ALM-005` 闹钟同步失败不阻止保存排班，但必须显示失败和重试入口。

---

## 13. 统计与导出

### 13.1 时间范围

- 本周、本月、上月、自定义日期范围。
- 默认按计薪周期统计，可切换自然月。

### 13.2 指标

- 应出勤、实际出勤、休息和法定节假日天数。
- 计划、实际、正常和加班工时。
- 迟到、早退、请假、缺卡和旷工。
- 预估工资、补贴、扣款、实发工资和差额。

### 13.3 图表

- 每日工时柱状图。
- 正常工时与加班工时构成图。
- 班次分布条形图。

所有图表下方必须提供数值明细，图表不能成为唯一信息来源。

### 13.4 导出

- V1.0 支持 CSV。
- 用户选择日期范围和字段后导出。
- 导出前提示文件可能包含工资和考勤敏感信息。
- 分享必须由用户主动触发。

---

## 14. AI 助理

### 14.1 能力

1. 对话聊天。
2. 回答 App 功能和使用方法。
3. 查询排班、出勤、工时、工资和闹钟状态。
4. 总结周/月数据和异常。
5. 通过对话提出排班、休息日、班次、闹钟等设置方案。
6. 经用户确认后执行软件设置。
7. 设置助理名称、性格、回复风格和虚拟人形象。

### 14.2 AI 界面

- 顶部：虚拟人、名称、在线/未配置状态、设置入口。
- 消息区：用户消息、AI 消息、统计卡和操作确认卡。
- 快捷问题：总结本月出勤、设置大小周、预估工资、检查闹钟。
- 底部：文字输入、附件预留和发送。

### 14.3 功能知识

AI 必须使用当前 App 版本提供的功能清单和帮助信息，不能仅凭模型常识猜测功能。未上线能力应明确说明“不支持”或“计划后续支持”。

### 14.4 统计行为

- AI 不自行计算工资和工时。
- App 先通过本地计算引擎生成准确结果，再交给 AI 总结。
- AI 回答的天数、工时和金额必须与统计页完全一致。
- 数据不足时明确指出缺卡、未配置工资等原因。

### 14.5 设置操作流程

```text
用户提出需求
→ AI 理解并生成结构化操作草案
→ App 校验
→ 展示修改前后预览和联动影响
→ 用户确认
→ App 执行
→ AI 返回真实结果和撤销入口
```

以下行为必须确认：

- 新增、修改或删除排班。
- 修改休息日、请假和节假日覆盖。
- 修改、启停或删除闹钟。
- 修改工资规则或已结算数据。
- 批量修改、覆盖未来排班或清空记录。

AI 不得在工具未返回成功时声称“已经设置完成”。

### 14.6 大模型连接设置

设置项：

- 接口类型：OpenAI 兼容接口、自定义 REST API。
- API 地址。
- API 密钥，默认掩码显示。
- 模型名称。
- 请求超时、最大输出长度和流式响应。
- 高级自定义请求头。
- 测试连接。

错误至少区分：地址无效、网络失败、TLS 失败、鉴权失败、模型不存在、限流、余额不足、响应不兼容和超时。

### 14.7 数据权限

用户分别控制：

- 排班读取。
- 出勤读取。
- 工资读取，默认关闭。
- 闹钟读取。
- 备注读取，默认关闭。
- 排班/闹钟写入，始终逐次确认。

默认优先向模型发送汇总结果，不发送完整原始记录。用户可以查看本次请求涉及的数据类型。

### 14.8 性格与虚拟人

- 预置温柔陪伴、简洁专业、活泼有趣。
- 支持自定义助理名称、语气、回复长度、主动提醒和表情频率。
- V1.0 使用内置静态 2D 头像或轻量动效。
- 性格只影响表达，不改变事实、数字、权限和确认流程。

### 14.9 AI 验收

- `F-AI-001` 未配置接口时显示配置引导，其他功能正常使用。
- `F-AI-002` 测试连接准确区分鉴权、模型、限流和网络错误。
- `F-AI-003` 用户说“下周六改休息”时，预览必须显示具体日期、原班次、新状态和闹钟影响。
- `F-AI-004` 未确认、确认过期或预览后数据已变化时，不得执行修改。
- `F-AI-005` 确认成功后日历、闹钟和统计同步更新，并提供撤销。
- `F-AI-006` 关闭工资权限后，模型不得获得工资金额。
- `F-AI-007` 性格切换不影响工具参数和计算结果。
- `F-AI-008` 要求跳过确认、读取密钥或执行代码的提示必须被拒绝。

---

## 15. 我的与设置

### 15.1 设置分类

| 分类 | 内容 |
|---|---|
| 工作设置 | 排班规则、班次模板、闹钟、工资、节假日 |
| AI 设置 | API、模型、数据权限、性格、虚拟人和对话记录 |
| 显示偏好 | 周起始日、24/12 小时制、工时格式、主题、语言 |
| 数据 | 备份、恢复、导出和清空 |
| 隐私 | App 锁预留、权限状态、隐私政策和诊断日志 |

### 15.2 数据操作

- 本地备份至少保留最近 7 个版本，可关闭自动备份。
- 恢复前展示备份日期、数据范围和当前数据覆盖风险。
- 清空数据必须二次确认。
- API 密钥不进入普通备份。
- 清除对话、AI 操作历史、模型配置和业务数据应提供独立入口。

---

## 16. 权限与平台差异

| 权限/能力 | 申请时机 | 拒绝后的行为 |
|---|---|---|
| 通知 | 首次开启闹钟 | 保留配置，显示无法提醒和设置入口 |
| 精确闹钟（Android） | 启用精确响铃 | 可退化为普通通知并提示风险 |
| 开机恢复（Android） | 安装声明 | 重启后重建闹钟 |
| AI 数据范围 | 首次使用对应数据 | 不发送该类数据，普通聊天仍可用 |
| 日历访问 | V1.0 不申请 | 无影响 |
| 定位 | 不申请 | 无影响 |

iOS 静音、专注模式和系统通知策略可能影响提醒效果，界面必须如实说明，不使用“绝对不会漏响”等承诺。

---

## 17. 空状态与错误状态

| 场景 | 页面行为 |
|---|---|
| 未配置排班 | 引导创建排班，日历不生成虚构工作日 |
| 未配置工资 | 展示工时但隐藏工资金额，提供设置入口 |
| 缺卡 | 显示缺卡并允许补录，不自动猜测时间 |
| 节假日未发布 | 标记为规则推算结果 |
| 闹钟权限不足 | 显示无法响铃和授权步骤 |
| AI 未配置 | 显示 API 配置引导 |
| AI 请求失败 | 保留用户输入，显示错误类型、重试和切换模型入口 |
| 数据冲突 | 展示本机/新数据差异，禁止静默覆盖 |
| 导出失败 | 保留选择条件并允许重新导出 |

---

## 18. 非功能体验要求

- 主流设备冷启动至可操作目标不超过 2.5 秒。
- 月历切换目标不超过 300 毫秒。
- 统计一年数据目标不超过 1 秒。
- 核心文字支持系统字体放大至 200%。
- 可点击区域不小于 iOS 44×44 pt、Android 48×48 dp。
- 支持浅色、深色模式。
- 状态不只依赖颜色表达。
- AI 流式回答应提供停止生成。
- 无网络不影响排班、打卡、统计和已注册闹钟。

---

## 19. 核心端到端验收场景

### 场景 A：大小周与闹钟联动

1. 用户设置本周为单休周，本周六白班，下周六休息。
2. 日历正确显示两个周六的不同状态。
3. 本周六生成白班闹钟，下周六不生成。
4. 用户将本周六手工改为休息。
5. 旧闹钟取消，月历和统计即时更新。

### 场景 B：夜班与工资

1. 用户设置夜班 20:00～次日 08:00，休息 60 分钟。
2. 夜班归属开始日，实际工时为 11 小时。
3. 正常工时上限为 8 小时，则加班为 3 小时。
4. 工资详情按用户配置的加班倍率计算并显示明细。

### 场景 C：AI 设置休息日

1. 用户对 AI 说“8 月 15 日改成休息，闹钟也关掉”。
2. AI 显示日期、白班→休息、闹钟 07:10→暂停。
3. 用户取消时不修改任何数据。
4. 用户再次发起并确认后，业务模块执行。
5. 日历、闹钟、统计同步更新，AI 提供撤销入口。

### 场景 D：数据权限

1. 用户允许 AI 读取排班，但关闭工资读取。
2. AI 可以回答班次问题。
3. 用户询问工资时，AI 说明未授权并提供授权入口。
4. 请求内容和日志均不包含工资金额。

---

## 20. 默认产品决策

- 产品暂定名为“班薪日历”，正式发布前可统一替换。
- V1.0 不强制注册账号，数据本地优先。
- 官方节假日默认开启，用户手工修改优先级最高。
- 夜班和跨月班次归属开始日。
- 闹钟由 App 自己管理，不修改系统时钟闹钟。
- 工资是个人预估，用户可录入实发金额核对差异。
- AI 属于可选增强模块，未配置模型不影响核心功能。
- AI 写操作始终先预览、再确认、后执行，并提供撤销。
- API 密钥只保存在系统安全存储，不进入普通数据库或备份。

---

## 21. 后续版本候选

### V1.1

- 桌面小组件、快捷打卡和 App 锁。
- 云同步、多设备恢复、PDF 报告。
- 系统日历同步。
- 工资条识别与差额核对。
- AI 语音输入和可选语音播报。

### V2.0

- 企业排班导入、换班和主管审批。
- 多份工作、多币种和团队排班。
- NFC、蓝牙或 Wi-Fi 辅助打卡。
- 3D 虚拟人、语音对话和口型动效。
