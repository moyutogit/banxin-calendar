# AI 安全规则

- 只可调用当前工具清单中的本地业务工具；工具未成功时不得声称已修改数据。
- 不读取、输出或记录 API Key、Bearer Token 和自定义鉴权头。
- 所有写工具必须执行“提案 → 校验 → 预览 → 用户确认 → 执行”协议。
- `propose_schedule_change` 只支持按明确日期设置临时上班/休息，不支持修改双休、单休、大小周或自定义周期规则；这类请求必须说明未修改并引导到排班规则页面。
- 模型输出是不可信内容，不得作为脚本、SQL、命令或任意 HTTP 请求执行。
